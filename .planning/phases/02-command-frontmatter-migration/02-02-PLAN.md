---
phase: 02-command-frontmatter-migration
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/add-todo.md
  - commands/gsd/check-todos.md
  - commands/gsd/debug.md
  - commands/gsd/discuss-phase.md
  - commands/gsd/execute-phase.md
  - commands/gsd/health.md
  - commands/gsd/new-milestone.md
  - commands/gsd/new-project.md
  - commands/gsd/plan-milestone-gaps.md
  - commands/gsd/quick.md
  - commands/gsd/reapply-patches.md
  - commands/gsd/resume-work.md
  - commands/gsd/settings.md
  - commands/gsd/update.md
autonomous: true

must_haves:
  truths:
    - "All 14 AskUserQuestion command files have opencode-native frontmatter"
    - "No `name:` field exists in any of the 14 files"
    - "All files use `tools:` YAML object format (not `allowed-tools:` list)"
    - "AskUserQuestion is NOT present in any tools section"
    - "No `@~/.claude/` path references remain in these files"
    - "No `/gsd:` slash command references remain in body text"
    - "debug.md body text has autonomous selection replacing AskUserQuestion instruction"
  artifacts:
    - path: "commands/gsd/debug.md"
      provides: "Migrated command with body text AskUserQuestion replacement"
      contains: "automatically select the most likely root cause"
    - path: "commands/gsd/reapply-patches.md"
      provides: "Migrated command with inline tools converted to YAML object"
      contains: "tools:"
    - path: "commands/gsd/new-project.md"
      provides: "Migrated command without AskUserQuestion"
      contains: "tools:"
  key_links:
    - from: "commands/gsd/*.md frontmatter"
      to: "opencode tool system"
      via: "tools: YAML object without AskUserQuestion"
      pattern: "tools:\\n  \\w+: true"
---

<objective>
Migrate frontmatter for the 14 command files that HAVE AskUserQuestion in their tools, removing AskUserQuestion from all tool lists. Also fix debug.md body text (CMD-06).

Purpose: Convert command frontmatter and remove all AskUserQuestion tool entries (part 2 of command migration — the critical interactivity removal).
Output: 14 command files with updated frontmatter, AskUserQuestion removed, debug.md body text fixed.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-agent-frontmatter-migration/01-01-SUMMARY.md

Reference the requirements spec for exact before/after formats:
@requirements/pilot-gsd-fork.md (Section 2: Command Files)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 7 command files (add-todo through health) — remove AskUserQuestion</name>
  <files>
    commands/gsd/add-todo.md
    commands/gsd/check-todos.md
    commands/gsd/debug.md
    commands/gsd/discuss-phase.md
    commands/gsd/execute-phase.md
    commands/gsd/health.md
    commands/gsd/new-milestone.md
  </files>
  <action>
For each of these 7 command files, apply all applicable transformations:

**Frontmatter changes:**
1. Remove `name:` field entirely
2. Convert `allowed-tools:` list to `tools:` YAML object. Tool name mapping:
   - `Read` → `read: true`
   - `Write` → `write: true`
   - `Edit` → `edit: true`
   - `Bash` → `bash: true`
   - `Grep` → `grep: true`
   - `Glob` → `glob: true`
   - `Task` → `task: true`
   - `WebSearch` → `websearch: true`
   - `WebFetch` → `webfetch: true`
   - `TodoWrite` → `todowrite: true`
   - `SlashCommand` → `slashcommand: true`
3. **CRITICAL: Remove `AskUserQuestion` entirely** — do NOT map it to anything. Just omit it.
4. Keep `description:` and `argument-hint:` unchanged

**Body text changes (all files):**
5. Replace all `@~/.claude/` with `@./.opencode/`
6. Replace all `/gsd:` with `/gsd-`
7. Replace all `~/.claude/` with `./.opencode/` in non-@ paths

**Special case — debug.md (CMD-06):**
8. In the body text (around line 53), find the text:
   `Use AskUserQuestion for each:`
   Replace with:
   `For each issue, automatically select the most likely root cause and proceed with investigation.`
   This is a body text change, NOT a frontmatter change. The frontmatter AskUserQuestion removal is separate.

**Specific frontmatter conversions (verify these):**
- `add-todo.md`: Read, Write, Bash → read, write, bash (AskUserQuestion removed)
- `check-todos.md`: Read, Write, Bash → read, write, bash (AskUserQuestion removed)
- `debug.md`: Read, Bash, Task → read, bash, task (AskUserQuestion removed)
- `discuss-phase.md`: Read, Write, Bash, Glob, Grep, Task → read, write, bash, glob, grep, task (AskUserQuestion removed)
- `execute-phase.md`: Read, Write, Edit, Glob, Grep, Bash, Task, TodoWrite → read, write, edit, glob, grep, bash, task, todowrite (AskUserQuestion removed)
- `health.md`: Read, Bash, Write → read, bash, write (AskUserQuestion removed)
- `new-milestone.md`: Read, Write, Bash, Task → read, write, bash, task (AskUserQuestion removed)
  </action>
  <verify>
```bash
for f in add-todo check-todos debug discuss-phase execute-phase health new-milestone; do
  echo "=== $f ==="
  grep -c 'name:' "commands/gsd/$f.md" || true
  grep -c 'allowed-tools' "commands/gsd/$f.md" || true
  grep -c 'AskUserQuestion' "commands/gsd/$f.md" || true
  grep -c '@~/.claude/' "commands/gsd/$f.md" || true
  grep -c '/gsd:' "commands/gsd/$f.md" || true
done
```
All counts should be 0 EXCEPT: debug.md AskUserQuestion count should be 0 (both frontmatter AND body text reference removed).

Also verify debug.md body text fix:
```bash
grep 'automatically select the most likely root cause' commands/gsd/debug.md
```
Should return a match.
  </verify>
  <done>7 command files have opencode-native frontmatter with AskUserQuestion removed from all tool lists, debug.md body text updated (CMD-06 satisfied)</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 7 command files (new-project through update) — remove AskUserQuestion</name>
  <files>
    commands/gsd/new-project.md
    commands/gsd/plan-milestone-gaps.md
    commands/gsd/quick.md
    commands/gsd/reapply-patches.md
    commands/gsd/resume-work.md
    commands/gsd/settings.md
    commands/gsd/update.md
  </files>
  <action>
For each of these 7 command files, apply all applicable transformations:

**Frontmatter changes:**
1. Remove `name:` field entirely
2. Convert `allowed-tools:` to `tools:` YAML object (same mapping as Task 1)
3. **CRITICAL: Remove `AskUserQuestion` entirely** — do NOT map it.
4. Keep `description:` and `argument-hint:` unchanged

**Special case — reapply-patches.md:**
This file uses INLINE format: `allowed-tools: Read, Write, Edit, Bash, Glob, Grep, AskUserQuestion`
Convert to multi-line YAML object format (same as all others), removing AskUserQuestion:
```yaml
tools:
  read: true
  write: true
  edit: true
  bash: true
  glob: true
  grep: true
```
Note: reapply-patches.md already has no `name:` field — only the tools format needs changing.

**Body text changes (all files):**
5. Replace all `@~/.claude/` with `@./.opencode/`
6. Replace all `/gsd:` with `/gsd-`
7. Replace all `~/.claude/` with `./.opencode/` in non-@ paths

**Specific frontmatter conversions (verify these):**
- `new-project.md`: Read, Bash, Write, Task → read, bash, write, task (AskUserQuestion removed)
- `plan-milestone-gaps.md`: Read, Write, Bash, Glob, Grep → read, write, bash, glob, grep (AskUserQuestion removed)
- `quick.md`: Read, Write, Edit, Glob, Grep, Bash, Task → read, write, edit, glob, grep, bash, task (AskUserQuestion removed)
- `reapply-patches.md`: Read, Write, Edit, Bash, Glob, Grep → read, write, edit, bash, glob, grep (AskUserQuestion removed; convert from inline to object format)
- `resume-work.md`: Read, Bash, Write, SlashCommand → read, bash, write, slashcommand (AskUserQuestion removed)
- `settings.md`: Read, Write, Bash → read, write, bash (AskUserQuestion removed)
- `update.md`: Bash → bash (AskUserQuestion removed)
  </action>
  <verify>
```bash
for f in new-project plan-milestone-gaps quick reapply-patches resume-work settings update; do
  echo "=== $f ==="
  grep -c 'name:' "commands/gsd/$f.md" || true
  grep -c 'allowed-tools' "commands/gsd/$f.md" || true
  grep -c 'AskUserQuestion' "commands/gsd/$f.md" || true
  grep -c '@~/.claude/' "commands/gsd/$f.md" || true
  grep -c '/gsd:' "commands/gsd/$f.md" || true
done
```
All counts should be 0.

Also verify reapply-patches.md has proper YAML object format:
```bash
head -10 commands/gsd/reapply-patches.md
```
Should show `tools:` with indented key-value pairs, NOT inline string.
  </verify>
  <done>7 command files have opencode-native frontmatter with AskUserQuestion removed, reapply-patches.md converted from inline to YAML object format</done>
</task>

</tasks>

<verification>
After both tasks complete, run comprehensive verification across all 14 files:

```bash
# No AskUserQuestion anywhere in any command file tools section
echo "=== AskUserQuestion in frontmatter check ==="
for f in commands/gsd/*.md; do
  # Check only frontmatter (between --- markers)
  awk '/^---$/{p++}p==1' "$f" | grep -l 'AskUserQuestion' 2>/dev/null && echo "FAIL: $(basename $f)"
done
# Should produce no output

# No name: fields
echo "=== name: check ==="
for f in add-todo check-todos debug discuss-phase execute-phase health new-milestone new-project plan-milestone-gaps quick reapply-patches resume-work settings update; do
  grep -l '^name:' "commands/gsd/$f.md" 2>/dev/null
done
# Should produce no output

# No allowed-tools in any file
echo "=== allowed-tools check ==="
grep -rl 'allowed-tools' commands/gsd/add-todo.md commands/gsd/check-todos.md commands/gsd/debug.md commands/gsd/discuss-phase.md commands/gsd/execute-phase.md commands/gsd/health.md commands/gsd/new-milestone.md commands/gsd/new-project.md commands/gsd/plan-milestone-gaps.md commands/gsd/quick.md commands/gsd/reapply-patches.md commands/gsd/resume-work.md commands/gsd/settings.md commands/gsd/update.md 2>/dev/null
# Should produce no output

# debug.md body text fix (CMD-06)
echo "=== CMD-06 check ==="
grep 'automatically select the most likely root cause' commands/gsd/debug.md
# Should return a match

# No AskUserQuestion at all in debug.md (both frontmatter and body)
echo "=== debug.md AskUserQuestion purge ==="
grep -c 'AskUserQuestion' commands/gsd/debug.md
# Should return 0
```
</verification>

<success_criteria>
- All 14 command files have `tools:` YAML object format
- No `name:` field in any of the 14 files
- No `allowed-tools:` in any of the 14 files
- AskUserQuestion does NOT appear in ANY command file's tools section
- debug.md body text updated with autonomous selection text (CMD-06)
- reapply-patches.md converted from inline to YAML object format
- No `@~/.claude/` references in any of the 14 files
- No `/gsd:` references in body text of any of the 14 files
- Requirements CMD-01, CMD-02, CMD-03, CMD-04, CMD-05, CMD-06 fully satisfied (combined with Plan 01)
</success_criteria>

<output>
After completion, create `.planning/phases/02-command-frontmatter-migration/02-02-SUMMARY.md`
</output>
