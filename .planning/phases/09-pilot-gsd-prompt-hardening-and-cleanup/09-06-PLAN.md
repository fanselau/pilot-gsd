---
phase: 09-prompt-hardening
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/command/gsd-execute-phase.md
  - .opencode/command/gsd-plan-phase.md
  - .opencode/command/gsd-help.md
  - .opencode/command/gsd-new-milestone.md
autonomous: true

must_haves:
  truths:
    - "Thick .opencode/command/ files that duplicate workflow content are slimmed to thin routing layers"
    - "commands/ (root) files remain the canonical thin routers — unchanged"
    - "gsd-planner agent decomposition is assessed and documented (action taken if beneficial)"
  artifacts:
    - path: ".opencode/command/gsd-execute-phase.md"
      provides: "Thin routing command (currently 340 lines, target: <80 lines)"
    - path: ".opencode/command/gsd-plan-phase.md"
      provides: "Thin routing command (currently 524 lines, target: <80 lines)"
  key_links:
    - from: ".opencode/command/gsd-execute-phase.md"
      to: ".opencode/get-shit-done/workflows/execute-phase.md"
      via: "@ reference delegation"
      pattern: "@\\./\\.opencode/get-shit-done/workflows/execute-phase\\.md"
    - from: ".opencode/command/gsd-plan-phase.md"
      to: ".opencode/get-shit-done/workflows/plan-phase.md"
      via: "@ reference delegation"
      pattern: "@\\./\\.opencode/get-shit-done/workflows/plan-phase\\.md"
---

<objective>
Deduplicate thick .opencode/command/ files that inline workflow content. Slim them to thin routing layers matching the pattern established in commands/ (root directory).

Purpose: The installer generates .opencode/command/ from commands/ source, but the current .opencode/command/ copies are bloated (e.g., gsd-execute-phase.md: 340 lines vs 39-line thin router, gsd-plan-phase.md: 524 lines vs 42-line thin router). These thick copies duplicate content that already lives in workflow files, wasting tokens when loaded. The requirements mandate: "Commands should be thin routing layers (description + args + which workflow to invoke)."
Output: Slimmed .opencode/command/ files that delegate to workflows via @ references instead of inlining the logic.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/prompt-hardening.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slim the worst .opencode/command/ offenders to thin routing layers</name>
  <files>
  - .opencode/command/gsd-execute-phase.md (340 lines → target ~40 lines)
  - .opencode/command/gsd-plan-phase.md (524 lines → target ~50 lines)
  - .opencode/command/gsd-help.md (481 lines → target ~25 lines)
  - .opencode/command/gsd-new-milestone.md (720 lines → target ~45 lines)
  </files>
  <action>
  For each thick .opencode/command/ file:

  1. Read the corresponding thin commands/ (root) file as the target pattern
  2. Read the thick .opencode/command/ file to identify what's inlined vs what's unique
  3. Rewrite the .opencode/command/ file to match the thin routing pattern:
     - Keep: frontmatter (description, argument-hint, tools) exactly as-is
     - Keep: objective section (brief description of what the command does)
     - Keep: execution_context with @ reference to the workflow file
     - Keep: context section with argument parsing and flag docs
     - Keep: any content UNIQUE to this file that isn't in the workflow (e.g., --auto flag doc in execute-phase)
     - Remove: any logic/steps that duplicate the workflow file content
     - Add: a <process> section that says "Execute the workflow from @ref end-to-end"

  The thin routing pattern (from commands/ root):
  ```
  ---
  [frontmatter]
  ---
  <objective>[Brief]</objective>
  <execution_context>@workflow-ref</execution_context>
  <context>[Args + flags]</context>
  <process>Execute the workflow end-to-end.</process>
  ```

  IMPORTANT: Preserve any flags or arguments that exist in the .opencode/command/ version but not in commands/ (root). The .opencode/command/ files may have extra flags like --auto that are valid.

  IMPORTANT: Preserve frontmatter exactly — the requirements forbid changing command frontmatter.
  </action>
  <verify>
  Check line counts:
  - `wc -l .opencode/command/gsd-execute-phase.md` — should be under 80 lines
  - `wc -l .opencode/command/gsd-plan-phase.md` — should be under 80 lines
  - `wc -l .opencode/command/gsd-help.md` — should be under 40 lines
  - `wc -l .opencode/command/gsd-new-milestone.md` — should be under 80 lines
  Verify @ references to workflow files are correct and targets exist.
  </verify>
  <done>4 worst .opencode/command/ offenders slimmed from combined ~2,065 lines to under ~240 lines. Each delegates to its workflow via @ reference.</done>
</task>

<task type="auto">
  <name>Task 2: Assess gsd-planner agent and audit remaining thick commands</name>
  <files>
  Audit (read-only analysis unless action taken):
  - .opencode/agents/gsd-planner.md (1,393 lines)
  - agents/gsd-planner.md (1,189 lines)
  - Remaining thick .opencode/command/ files

  May modify if dedup opportunities found:
  - Additional .opencode/command/ files exceeding 3x their commands/ counterpart
  </files>
  <action>
  **Part A: gsd-planner agent assessment**

  The requirements "Nice to Have" section mentions decomposing gsd-planner.md (10.5K tokens). The "Must Have" section says: "Check gsd-planner agent vs planning workflow content."

  1. Read .opencode/agents/gsd-planner.md and .opencode/get-shit-done/workflows/plan-phase.md
  2. Identify content overlap between the agent prompt and the workflow
  3. If significant overlap exists (>100 lines of duplicated logic): extract the duplicated content into a reference file at `.opencode/get-shit-done/references/planning-methodology.md` and replace the duplication in the agent with an @ reference
  4. If no significant overlap: document this finding in the summary — "gsd-planner agent and plan-phase workflow are complementary, not duplicated"

  **Part B: Remaining thick .opencode/command/ audit**

  Check all remaining .opencode/command/ files that are >3x their commands/ counterpart:
  - For each: compare content against the corresponding workflow file
  - If the file inlines workflow logic that already exists in the workflow: slim it to a thin router
  - If the file has unique content not in the workflow: keep the unique content, remove only the duplicated parts
  - Prioritize the largest offenders; skip files under 200 lines (diminishing returns)

  The following are the remaining large offenders (>200 lines):
  - .opencode/command/gsd-remove-phase.md (348 lines)
  - .opencode/command/gsd-progress.md (363 lines)
  - .opencode/command/gsd-quick.md (313 lines)
  - .opencode/command/gsd-audit-milestone.md (276 lines)
  - .opencode/command/gsd-new-project.md (282 lines)
  - .opencode/command/gsd-plan-milestone-gaps.md (294 lines)
  - .opencode/command/gsd-check-todos.md (227 lines)
  - .opencode/command/gsd-insert-phase.md (226 lines)
  - .opencode/command/gsd-verify-work.md (218 lines)

  Slim each to a thin routing layer following the same pattern as Task 1.
  </action>
  <verify>
  Run: `for f in .opencode/command/gsd-*.md; do echo "$(wc -l < "$f") $f"; done | sort -rn | head -10`
  Expected: No .opencode/command/ file exceeds 150 lines (target: all are thin routers under ~80 lines).
  Verify: @ references in slimmed files point to valid workflow files.
  </verify>
  <done>All .opencode/command/ files are thin routing layers. gsd-planner agent assessed for dedup (action taken or documented). Total token savings documented in summary.</done>
</task>

</tasks>

<verification>
1. `for f in .opencode/command/gsd-*.md; do echo "$(wc -l < "$f") $f"; done | sort -rn | head -5` — no file exceeds 150 lines
2. All @ references to workflow files are valid (workflows exist)
3. Command frontmatter preserved in all modified files
4. gsd-planner agent dedup assessed and documented
</verification>

<success_criteria>
- All .opencode/command/ files are thin routing layers (under ~80-100 lines each)
- Total line reduction documented (estimated: ~5,000+ lines removed)
- gsd-planner agent overlap with plan-phase workflow assessed
- Command frontmatter unchanged in all files
- All @ references to workflows resolve correctly
</success_criteria>

<output>
After completion, create `.planning/phases/09-pilot-gsd-prompt-hardening-and-cleanup/09-06-SUMMARY.md`
</output>
