---
phase: 09-prompt-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/agents/gsd-codebase-mapper.md
  - .opencode/agents/gsd-debugger.md
  - .opencode/agents/gsd-executor.md
  - .opencode/agents/gsd-integration-checker.md
  - .opencode/agents/gsd-phase-researcher.md
  - .opencode/agents/gsd-plan-checker.md
  - .opencode/agents/gsd-planner.md
  - .opencode/agents/gsd-project-researcher.md
  - .opencode/agents/gsd-research-synthesizer.md
  - .opencode/agents/gsd-roadmapper.md
  - .opencode/agents/gsd-verifier.md
autonomous: true

must_haves:
  truths:
    - "Zero negative instruction patterns remain in .opencode/agents/ directory"
    - "Every rewritten instruction preserves the original behavioral intent"
    - "Agent files remain valid markdown with correct frontmatter"
  artifacts:
    - path: ".opencode/agents/gsd-debugger.md"
      provides: "Debugger agent with ~20 negative instructions rewritten"
    - path: ".opencode/agents/gsd-planner.md"
      provides: "Planner agent with ~7 negative instructions rewritten"
  key_links:
    - from: ".opencode/agents/*.md"
      to: "behavioral instructions"
      via: "positive framing"
      pattern: "Use .* instead|Always |Prefer |Reserve .* for|Only use"
---

<objective>
Rewrite all negative instructions to positive framing in the 11 agent files in `.opencode/agents/` (~30 instances total).

Purpose: These are a separate copy of agent files used by opencode internally. They contain the same types of negative instructions and need the same positive-framing treatment as the root agents/ directory.
Output: 11 agent files with all negative instructions converted to positive equivalents.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/prompt-hardening.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite negative instructions in .opencode/agents/ (high-count files)</name>
  <files>
  - .opencode/agents/gsd-debugger.md (~20 instances)
  - .opencode/agents/gsd-phase-researcher.md (~14 instances)
  - .opencode/agents/gsd-roadmapper.md (~10 instances)
  - .opencode/agents/gsd-plan-checker.md (~9 instances)
  - .opencode/agents/gsd-project-researcher.md (~8 instances)
  </files>
  <action>
  For each file, find every instance of "don't", "never", "do NOT", "do not", "must not", "avoid" that is an INSTRUCTION.

  Skip non-instruction uses:
  - Markdown headings or section titles
  - Text inside code blocks (``` delimited)
  - Example content / template examples
  - Descriptive text (e.g., "this avoids race conditions")

  Rewrite each instruction using positive framing. Conversion patterns:
  - "Don't use X for Y" → "Use Z for Y. Reserve X for W."
  - "Never skip X" → "Always run X before proceeding."
  - "Do NOT include X" → "Include only Y and Z."
  - "Avoid X" → "Prefer Y instead."
  - "Must not X" → "Keep X unchanged."

  Preserve original behavioral intent exactly. "Do NOT" section headers should become "Constraints" or "Guardrails" with positively-framed rules.

  Note: These files are DIFFERENT from the root agents/ files (different line counts, different content). Read each file fresh — do not assume they match root agents/.
  </action>
  <verify>
  Run: `rg -ci "(don't|never |do not|must not|avoid )" .opencode/agents/gsd-debugger.md .opencode/agents/gsd-phase-researcher.md .opencode/agents/gsd-roadmapper.md .opencode/agents/gsd-plan-checker.md .opencode/agents/gsd-project-researcher.md`
  Expected: 0 for each file.
  </verify>
  <done>5 high-count .opencode/agents/ files have zero negative instruction patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite negative instructions in .opencode/agents/ (remaining files)</name>
  <files>
  - .opencode/agents/gsd-codebase-mapper.md (~7 instances)
  - .opencode/agents/gsd-planner.md (~7 instances)
  - .opencode/agents/gsd-executor.md (~6 instances)
  - .opencode/agents/gsd-verifier.md (~6 instances)
  - .opencode/agents/gsd-research-synthesizer.md (~2 instances)
  - .opencode/agents/gsd-integration-checker.md (~1 instance)
  </files>
  <action>
  Same approach as Task 1.

  1. Find all negative instruction patterns
  2. Skip non-instruction uses
  3. Rewrite to positive framing
  4. Preserve behavioral intent

  After all files, final sweep: `rg "(don't|never |do not|must not|avoid )" .opencode/agents/ --type md` to catch anything missed.
  </action>
  <verify>
  Run: `rg -ci "(don't|never |do not|must not|avoid )" .opencode/agents/ --type md | rg -v ':0$'`
  Expected: zero files with non-zero counts.
  </verify>
  <done>All 11 .opencode/agents/ files have zero negative instruction patterns. Full directory sweep confirms clean.</done>
</task>

</tasks>

<verification>
1. `rg -c "(don't|never |do not|must not|avoid )" .opencode/agents/ --type md | rg -v ':0$' | wc -l` returns 0
2. Spot-check 3 agent files for valid frontmatter
3. Behavioral intent preserved in all rewrites
</verification>

<success_criteria>
- Zero negative instruction patterns in .opencode/agents/
- All 11 agent files modified with positive framing
- Frontmatter intact, file structure preserved
</success_criteria>

<output>
After completion, create `.planning/phases/09-pilot-gsd-prompt-hardening-and-cleanup/09-03-SUMMARY.md`
</output>
