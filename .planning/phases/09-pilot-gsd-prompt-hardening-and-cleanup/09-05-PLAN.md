---
phase: 09-prompt-hardening
plan: 05
type: execute
wave: 2
depends_on: ["09-01", "09-02", "09-03", "09-04a", "09-04b", "09-06"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "grep for /home/ returns zero results outside .git and .planning"
    - "grep for negative instructions returns zero results across all prompt files"
    - "Every @ reference resolves to an existing file"
    - "All dead files are confirmed deleted"
  artifacts: []
  key_links: []
---

<objective>
Run comprehensive validation checks across the entire prompt codebase to confirm all Phase 9 objectives are met.

Purpose: Final gate ensuring the cleanup work from plans 01-04b and 06 is complete and nothing was missed. Produces a clear pass/fail report.
Output: Validated clean state with documented evidence.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/prompt-hardening.md
@.planning/phases/09-pilot-gsd-prompt-hardening-and-cleanup/09-01-SUMMARY.md
@.planning/phases/09-pilot-gsd-prompt-hardening-and-cleanup/09-02-SUMMARY.md
@.planning/phases/09-pilot-gsd-prompt-hardening-and-cleanup/09-03-SUMMARY.md
@.planning/phases/09-pilot-gsd-prompt-hardening-and-cleanup/09-04a-SUMMARY.md
@.planning/phases/09-pilot-gsd-prompt-hardening-and-cleanup/09-04b-SUMMARY.md
@.planning/phases/09-pilot-gsd-prompt-hardening-and-cleanup/09-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive validation sweep</name>
  <files>No files modified — read-only validation</files>
  <action>
  Run ALL validation checks specified in the requirements doc. For each check, log PASS or FAIL with evidence.

  **Check 1: Zero hardcoded home paths**
  `grep -rn '/home/' . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=node_modules --exclude-dir=requirements`
  Expected: zero results
  Note: Phase 3 already fixed this, but confirm it hasn't regressed. The requirements/ directory is excluded because requirements/prompt-hardening.md contains `/home/` in its documentation text (describing what was fixed), which is not a hardcoded path in a prompt file.

  **Check 2: Zero negative instructions in prompt files**
  `rg -c "(don't|never |do not|must not|avoid )" agents/ commands/ .opencode/agents/ .opencode/command/ .opencode/get-shit-done/ --type md | rg -v ':0$'`
  Expected: zero files with non-zero counts.
  If any remain, document each one and whether it's a genuine instruction (FAIL) or legitimate non-instruction context (PASS with note).

  **Check 3: All @ references resolve**
  For every file containing `@./` pattern, extract the referenced path and verify the file exists.
  `rg '@\.\/' agents/ commands/ .opencode/ --type md -o --no-filename | sort -u`
  For each unique path: `test -f "$path" && echo "OK: $path" || echo "BROKEN: $path"`
  Expected: zero BROKEN entries.

  **Check 4: Dead files confirmed deleted**
  Verify these directories have the expected file counts:
  - `ls .opencode/get-shit-done/references/ | wc -l` should be 10
  - `ls .opencode/get-shit-done/templates/*.md | wc -l` should be ~11
  - `ls .opencode/get-shit-done/templates/codebase/ 2>/dev/null` should not exist
  - `ls .opencode/get-shit-done/templates/config.json 2>/dev/null` should not exist

  **Check 5: Command frontmatter intact**
  Spot-check 5 command files to verify frontmatter (description, argument-hint, tools) is unchanged from pre-phase-9 state.

  **Check 6: No broken workflow references**
  Verify the three previously-flagged workflow files exist:
  - `.opencode/get-shit-done/workflows/add-phase.md`
  - `.opencode/get-shit-done/workflows/plan-phase.md`
  - `.opencode/get-shit-done/workflows/new-milestone.md`

  Produce a summary report with PASS/FAIL for each check.
  </action>
  <verify>
  All 6 checks pass. Any FAIL results are documented with specific files and line numbers for follow-up.
  </verify>
  <done>All validation checks pass. Phase 9 objectives confirmed complete: zero hardcoded paths, zero negative instructions, zero broken references, all dead files removed.</done>
</task>

</tasks>

<verification>
All 6 validation checks from requirements doc section "Validation After Changes" pass:
1. `grep -rn '/home/' . --exclude-dir=.git` returns zero results (excluding .planning/)
2. Every @ reference resolves to an existing file
3. Zero negative instruction patterns in prompt files
4. Dead files confirmed removed
5. Command frontmatter intact
6. Workflow files exist
</verification>

<success_criteria>
- All 6 validation checks pass with documented evidence
- Phase 9 requirements fully satisfied
- Summary report produced
</success_criteria>

<output>
After completion, create `.planning/phases/09-pilot-gsd-prompt-hardening-and-cleanup/09-05-SUMMARY.md`
</output>
