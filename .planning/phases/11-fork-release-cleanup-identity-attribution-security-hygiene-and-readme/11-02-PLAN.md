---
phase: 11-fork-release-cleanup-identity-attribution-security-hygiene-and-readme
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/update.md
autonomous: true
requirements: [HYGIENE-01, HYGIENE-02, HYGIENE-03, HYGIENE-04, HYGIENE-05, SEC-01, SEC-02, SEC-03, SEC-04]

must_haves:
  truths:
    - "No accidentally committed secrets exist in tracked tree"
    - "Interactive AskUserQuestion patterns in workflows are all documented as intentional with auto-mode guards"
    - "update.md does not link to upstream changelog for fork-specific updates"
    - "No deprecated artifacts confuse the public surface"
  artifacts:
    - path: "get-shit-done/workflows/update.md"
      provides: "Fixed update workflow with fork-appropriate links"
    - path: ".planning/phases/11-fork-release-cleanup-identity-attribution-security-hygiene-and-readme/11-02-SUMMARY.md"
      provides: "Audit results and cleanup documentation"
  key_links:
    - from: "get-shit-done/workflows/update.md"
      to: "CHANGELOG.md"
      via: "changelog link"
      pattern: "lucafanselau/pilot-gsd"
---

<objective>
Security scan + fork hygiene audit — scan for secrets, audit interactive patterns, fix stale upstream references in workflows, and clean up deprecated artifacts.

Purpose: The repo must be clean of leaked secrets and confusing upstream references before public launch. Interactive patterns from upstream that conflict with autonomy-first positioning need explicit documentation or removal.

Output: Secret scan report (clean or with remediation plan). All interactive patterns documented. Stale upstream links fixed.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/fork-release-cleanup-and-readme.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Security scan and fork hygiene audit</name>
  <files>get-shit-done/workflows/update.md</files>
  <action>
**Part A — Security Scan:**

Run a comprehensive secret scan across the entire tracked tree:

```bash
# Check for common secret patterns
rg -l '(sk-[a-zA-Z0-9]{20,}|ghp_[a-zA-Z0-9]{36}|gho_[a-zA-Z0-9]{36}|AKIA[0-9A-Z]{16}|-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY)' --type-not binary
rg -l '(xox[baprs]-[0-9a-zA-Z]{10,}|hooks\.slack\.com/services/)' --type-not binary
rg -l '(AIza[0-9A-Za-z_-]{35}|[0-9a-f]{32}\.atlassian\.net)' --type-not binary
# Check .env files
find . -name '.env*' -not -path './.git/*' -not -path './node_modules/*'
# Check for base64-encoded secrets (long base64 strings in config files)
rg '[A-Za-z0-9+/]{40,}={0,2}' --type json --type yaml -l 2>/dev/null || true
```

If ANY secrets found: sanitize the tracked file (replace with placeholder or remove). If the secret only exists in git history (not current tree), create a short remediation note in the SUMMARY documenting: what was found, whether it's in tracked history or only current tree, recommended cleanup steps, risks of history rewrite. Per requirements: NO git history rewrite in this phase.

If no secrets found: document "Clean — no tracked secrets found" in SUMMARY.

**Part B — AskUserQuestion Audit:**

Grep for all `AskUserQuestion` patterns in get-shit-done/ directory. For each occurrence, verify it has one of:
1. An `**If interactive:**` or `**If auto mode:**` guard (already done in Phases 4, 10)
2. Is in an inherently interactive workflow (discuss-phase.md, settings.md, new-project.md user-facing paths)

Document findings in SUMMARY: list every file with AskUserQuestion, its guard status, and whether it's intentional.

Prior work confirms: Phase 4 stripped/guarded all autonomous-pipeline workflows. Phase 10 re-verified. The audit here is the final confirmation for the public surface.

**Part C — Stale Upstream Links:**

Fix `get-shit-done/workflows/update.md` line 186: change `https://github.com/glittercowboy/get-shit-done/blob/main/CHANGELOG.md` to `https://github.com/lucafanselau/pilot-gsd/blob/main/CHANGELOG.md`. This is the update workflow that shows users where to find the changelog — it must point to the fork's changelog, not upstream.

**Part D — Deprecated Artifact Audit:**

Check for artifacts that should not be part of the public surface:
1. `ls .github/workflows/` — verify auto-label-issues.yml is reasonable for this fork (it is — generic issue labeling)
2. `ls .github/ISSUE_TEMPLATE/` — verify templates are updated (bug_report.yml was fixed in Plan 01)
3. Check for any leftover `/gsd:` slash command references: `rg '/gsd:' --include '*.md' --include '*.yml' --include '*.json'`
4. Check for stale `~/.claude/` path references: `rg '~/\.claude/' --include '*.md' --include '*.yml'`

If `/gsd:` or `~/.claude/` patterns found in public-facing files, fix them. Prior phases should have caught these, but this is the final sweep.

Document everything found (or not found) in the SUMMARY.
  </action>
  <verify>
1. Secret scan commands return no results (or remediation plan documented)
2. `grep "glittercowboy" get-shit-done/workflows/update.md` returns no matches
3. `grep "lucafanselau/pilot-gsd" get-shit-done/workflows/update.md` returns a match
4. `rg '/gsd:' --include '*.md' -l` returns no matches in public-facing files (planning files may have historical references — those are fine)
  </verify>
  <done>Security scan complete (clean or with remediation plan). All AskUserQuestion patterns documented. Stale upstream links fixed in update.md. No deprecated artifacts found or all cleaned up.</done>
</task>

</tasks>

<verification>
- Secret scan returns clean results
- `grep -rn "glittercowboy" get-shit-done/workflows/update.md` returns 0 matches
- AskUserQuestion audit documented in SUMMARY
- No unexpected deprecated artifacts in public surface
</verification>

<success_criteria>
No tracked secrets in repository. All interactive patterns either guarded or documented as intentional. update.md points to fork changelog. Public surface has no confusing deprecated artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/11-fork-release-cleanup-identity-attribution-security-hygiene-and-readme/11-02-SUMMARY.md`
</output>
