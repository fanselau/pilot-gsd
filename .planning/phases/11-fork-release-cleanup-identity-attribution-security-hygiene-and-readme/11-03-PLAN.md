---
phase: 11-fork-release-cleanup-identity-attribution-security-hygiene-and-readme
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - README.md
autonomous: true
requirements: [README-01, README-02, README-03, README-04, VERIFY-01, VERIFY-02, VERIFY-03]

must_haves:
  truths:
    - "README clearly communicates this is a distinct autonomy-first system inspired by GSD"
    - "README explains what changed from upstream in practical terms"
    - "README explains who it is for and what the install/use path is"
    - "README does not read like 'we added one feature to upstream'"
    - "README has respectful, explicit attribution to the original project"
    - "Existing tests pass"
    - "Public-facing docs are internally consistent (version, name, links match)"
  artifacts:
    - path: "README.md"
      provides: "Complete fork-positioned README"
      min_lines: 80
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "version and name consistency"
      pattern: "0\\.1\\.0"
    - from: "README.md"
      to: "https://github.com/gsd-build/get-shit-done"
      via: "attribution link"
      pattern: "gsd-build/get-shit-done"
    - from: "README.md"
      to: "https://github.com/lucafanselau/pilot"
      via: "parent project link"
      pattern: "lucafanselau/pilot"
---

<objective>
Rewrite README.md to position pilot-gsd as a deliberate, autonomy-first fork of Get Shit Done. Run final verification (tests, consistency check).

Purpose: The README is the project's primary public face. It must clearly communicate what pilot-gsd is, why it exists as a fork, what's different, who it's for, and how to use it — without reading as a trivial fork or erasing credit.

Output: Complete, well-structured README.md. Verified test suite passes. Cross-document consistency confirmed.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/fork-release-cleanup-and-readme.md
@.planning/phases/11-fork-release-cleanup-identity-attribution-security-hygiene-and-readme/11-01-SUMMARY.md
@.planning/phases/11-fork-release-cleanup-identity-attribution-security-hygiene-and-readme/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md using project README skills</name>
  <files>README.md</files>
  <action>
Use the installed README skills for guidance:
- `.opencode/skills/crafting-effective-readmes/SKILL.md` — project type is "Open Source fork" (audience: contributors, users, pilot users)
- `.opencode/skills/create-readme/SKILL.md` — structure and tone guidance

Read the existing README.md and the current project state (package.json for version/identity, the requirements file for what the README must communicate).

**Rewrite README.md with this structure:**

1. **Title and one-liner:** `# pilot-gsd` followed by a one-sentence description: what it is (autonomy-first GSD system), who it's for (Pilot autonomous development pipeline), where it came from (forked from Get Shit Done).

2. **What This Is (2-3 paragraphs):** 
   - pilot-gsd is an autonomy-first development system built on top of GSD (Get Shit Done), the meta-prompting and context engineering framework by TÂCHES.
   - Explain the key difference: GSD is designed for human-interactive use with Claude Code/OpenCode/Gemini. pilot-gsd strips all interactive prompts and makes every workflow autonomous — it's designed to be driven by a machine (Pilot), not a human at a terminal.
   - This is a real fork, not a thin wrapper. It modifies agent behavior, workflow control flow, frontmatter format, path conventions, and default configuration.

3. **Key Differences from Upstream GSD:**
   Use a clear comparison format. Include:
   - Zero interactivity (all AskUserQuestion removed or auto-mode guarded)
   - Explicit model assignments (opus for creators, sonnet for checkers)
   - opencode-native frontmatter format
   - Autonomous config defaults (yolo mode, all gates off)
   - New commands: gsd-delegate (runner integration), gsd-judge (execution verdict)
   - Path convention: `.opencode/` instead of `~/.claude/`
   - Slash commands: `/gsd-*` instead of `/gsd:*`

4. **How It Works:**
   - Used as a git submodule by [Pilot](https://github.com/lucafanselau/pilot)
   - `pilot setup` symlinks into project `.opencode/` directories
   - Commands become available as `/gsd-*` slash commands
   - The delegation prompt (`gsd-delegate.md`) reads project state and outputs execution plans

5. **Repository Structure:**
   Keep the existing tree diagram but verify it's accurate. Update command/agent counts if they changed after Phase 10 upstream sync.

6. **Available Commands:**
   Keep the existing tables — they're already good. Just verify accuracy.

7. **Agent Architecture:**
   Keep the existing table.

8. **Installation:**
   - Clear statement: this repo is used as a submodule of Pilot, not installed directly
   - Link to Pilot repo
   - Brief note about the `get-shit-done-cc` bin name being inherited from upstream for compatibility

9. **Attribution:**
   - Forked from [get-shit-done](https://github.com/gsd-build/get-shit-done) by TÂCHES (Lex Christopherson)
   - Link to upstream repo and upstream README
   - Thank the original project explicitly
   - State the license (MIT)

**Style guidance (from requirements):**
- Keep it concrete and credible — no vague marketing fluff
- Do not use excessive emojis
- Use GitHub Flavored Markdown with admonition syntax where appropriate (e.g., `> [!NOTE]`)
- The README must NOT read like "we added one feature to upstream" — explain the larger autonomy/fork direction honestly

**Cross-reference Plan 01 outputs:** version should be `0.1.0` (from package.json update). Links to fork repo should use `lucafanselau/pilot-gsd`. Links to upstream should use `gsd-build/get-shit-done`.
  </action>
  <verify>
1. README.md exists and has >80 lines
2. `grep "autonomy" README.md` shows the positioning
3. `grep "TÂCHES\|Lex Christopherson\|gsd-build/get-shit-done" README.md` shows attribution
4. `grep "lucafanselau/pilot" README.md` links to parent project
5. `grep "0\.1\.0\|pilot-gsd" README.md` shows fork identity
6. README does NOT contain misleading phrases like "simple fork" or "thin wrapper"
  </verify>
  <done>README.md fully rewritten with clear fork positioning, practical differences explained, attribution preserved, and no marketing fluff.</done>
</task>

<task type="auto">
  <name>Task 2: Final verification — tests and cross-document consistency</name>
  <files>README.md, package.json, CHANGELOG.md, SECURITY.md, .github/CODEOWNERS, .github/FUNDING.yml</files>
  <action>
**Part A — Run tests:**

```bash
npm test
```

All existing tests must pass. This phase only touches documentation and metadata — no functional code changes. Tests should pass unchanged.

**Part B — Cross-document consistency check:**

Verify these facts are consistent across all updated documents:

1. **Version:** `package.json` says `0.1.0`, `CHANGELOG.md` has `[0.1.0]` section, README mentions the version (or doesn't reference a specific version, which is also fine).

2. **Project name:** `package.json` says `pilot-gsd`, README title says `pilot-gsd`, CHANGELOG header says pilot-gsd fork.

3. **Links:** 
   - Fork repo: `github.com/lucafanselau/pilot-gsd` — used in package.json homepage, CHANGELOG fork links, README fork links
   - Upstream repo: `github.com/gsd-build/get-shit-done` — used in README attribution, CHANGELOG upstream links
   - Parent project: `github.com/lucafanselau/pilot` — used in README

4. **Owner references:**
   - `.github/CODEOWNERS` → `@lucafanselau` (not `@glittercowboy`)
   - `.github/FUNDING.yml` → `github: lucafanselau` (not `glittercowboy`)
   - `SECURITY.md` → fork contact (not `security@gsd.build`)
   - `LICENSE` → `Lex Christopherson` (preserved — this is correct)

5. **No stale upstream owner references in fork-owned files:**
   ```bash
   grep -rn "glittercowboy" SECURITY.md .github/CODEOWNERS .github/FUNDING.yml README.md
   ```
   Should return 0 matches.

   ```bash
   grep -rn "glittercowboy" CHANGELOG.md | head -5
   ```
   Should only appear in upstream-era release links (which correctly point to the upstream repo).

If any inconsistency found, fix it.

**Part C — Final read-through:**

Read README.md start to finish. Verify it:
- Opens with clear positioning (not vague)
- Explains the fork direction honestly
- Does not read like "we added one feature"
- Has respectful attribution
- Is internally consistent
  </action>
  <verify>
1. `npm test` exits 0
2. `grep -c "glittercowboy" SECURITY.md .github/CODEOWNERS .github/FUNDING.yml README.md` shows 0 for each
3. `node -e "console.log(require('./package.json').version)"` outputs `0.1.0`
4. `grep "0.1.0" CHANGELOG.md` confirms version entry
5. `grep "TÂCHES" README.md CHANGELOG.md` confirms attribution in both
  </verify>
  <done>Tests pass. All public-facing documents are internally consistent. Project reads as a deliberate forked product with clean attribution and clear identity.</done>
</task>

</tasks>

<verification>
- `npm test` passes
- README.md communicates fork positioning clearly
- All documents reference consistent version, name, and links
- Attribution to upstream project is preserved and respectful
- No misleading upstream-owner references in fork-owned files
</verification>

<success_criteria>
README.md is a polished, honest, concrete description of pilot-gsd as an autonomy-first GSD fork. Tests pass. All public-facing docs are consistent. The project clearly reads as a deliberate forked product with clean attribution and clear identity.
</success_criteria>

<output>
After completion, create `.planning/phases/11-fork-release-cleanup-identity-attribution-security-hygiene-and-readme/11-03-SUMMARY.md`
</output>
