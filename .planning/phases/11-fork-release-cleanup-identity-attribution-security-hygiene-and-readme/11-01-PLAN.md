---
phase: 11-fork-release-cleanup-identity-attribution-security-hygiene-and-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY.md
  - .github/CODEOWNERS
  - .github/FUNDING.yml
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - package.json
  - CHANGELOG.md
autonomous: true
requirements: [IDENT-01, IDENT-02, IDENT-03, IDENT-04, IDENT-05, HYGIENE-05]

must_haves:
  truths:
    - "SECURITY.md has fork-appropriate contact info, not upstream email"
    - "CODEOWNERS and FUNDING.yml reference fork owner, not upstream owner"
    - "package.json version uses fork-distinct scheme (0.x.y not 1.20.x)"
    - "CHANGELOG.md release links point to fork repo for fork-era entries"
    - "Bug report template references fork package name"
    - "Attribution to original project is preserved in LICENSE and relevant docs"
  artifacts:
    - path: "SECURITY.md"
      provides: "Fork-appropriate security contact"
      contains: "lucafanselau"
    - path: ".github/CODEOWNERS"
      provides: "Fork code ownership"
      contains: "lucafanselau"
    - path: ".github/FUNDING.yml"
      provides: "Fork funding config"
      contains: "lucafanselau"
    - path: "package.json"
      provides: "Fork identity and distinct version"
    - path: "CHANGELOG.md"
      provides: "Clean changelog with fork section and correct links"
    - path: ".github/ISSUE_TEMPLATE/bug_report.yml"
      provides: "Fork-appropriate bug report template"
    - path: "LICENSE"
      provides: "Original attribution preserved (Lex Christopherson)"
      contains: "Lex Christopherson"
  key_links:
    - from: "package.json"
      to: "README.md"
      via: "version and name consistency"
      pattern: "pilot-gsd"
    - from: "CHANGELOG.md"
      to: "github.com/lucafanselau/pilot-gsd"
      via: "release links for fork entries"
      pattern: "lucafanselau/pilot-gsd"
---

<objective>
Clean up project identity files — replace misleading upstream-owner references with fork identity while preserving attribution to the original project.

Purpose: The public-facing metadata (SECURITY.md, CODEOWNERS, FUNDING.yml, package.json, CHANGELOG.md, issue templates) still references the upstream owner/project. Users filing issues, reporting vulnerabilities, or checking versions get directed to the wrong project.

Output: All metadata files reflect pilot-gsd fork identity. Version scheme is distinct from upstream.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/fork-release-cleanup-and-readme.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update project metadata files with fork identity</name>
  <files>SECURITY.md, .github/CODEOWNERS, .github/FUNDING.yml, .github/ISSUE_TEMPLATE/bug_report.yml</files>
  <action>
**SECURITY.md:** Replace the upstream security contact. Change `security@gsd.build` and `@glittercowboy` references to point to the fork's GitHub Security Advisories page and fork owner contact. Keep the structure (reporting instructions, response timeline, scope) but make it clear this is pilot-gsd's security policy. Add a note: "For vulnerabilities in the upstream GSD project, report to the original project at https://github.com/gsd-build/get-shit-done." This preserves attribution while directing fork-specific reports correctly.

**.github/CODEOWNERS:** Replace `@glittercowboy` with `@lucafanselau`.

**.github/FUNDING.yml:** Replace `github: glittercowboy` with `github: lucafanselau`.

**.github/ISSUE_TEMPLATE/bug_report.yml:** 
- Change the version check instruction from `npm list -g get-shit-done-cc` to `cat package.json | grep version` (since pilot-gsd is used as a submodule, not globally installed via npm).
- Update placeholder to show a pilot-gsd version example (e.g., `0.1.0`).
- Keep the rest of the template intact.

**Installer/banner audit:** Read `bin/install.js` and audit the banner text (lines ~130-140) and help text (line ~174). The banner says "Get Shit Done" and "by TÂCHES" — this is the shared upstream installer and changing it would break upstream compatibility. The `bin` name `get-shit-done-cc` is also inherited. Document in the SUMMARY that these are intentionally preserved for compatibility and the README will explain this. Verify the installer's fork-comment (added in Phase 5, INST-01) is still present.

**IMPORTANT:** The LICENSE file MUST remain unchanged — it preserves the original copyright holder (Lex Christopherson) per MIT license terms and the requirement to preserve attribution.
  </action>
  <verify>
1. `grep -c "glittercowboy" SECURITY.md .github/CODEOWNERS .github/FUNDING.yml` returns 0 for each file
2. `grep "lucafanselau" .github/CODEOWNERS` returns a match
3. `grep "lucafanselau" .github/FUNDING.yml` returns a match
4. `grep "upstream GSD" SECURITY.md` confirms attribution preserved
5. `grep "Lex Christopherson" LICENSE` confirms license untouched
6. `grep "pilot-gsd" bin/install.js` confirms fork comment present (from Phase 5, INST-01)
  </verify>
  <done>All metadata files reference fork owner. Attribution to upstream preserved in SECURITY.md note and LICENSE.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt fork versioning and clean up CHANGELOG</name>
  <files>package.json, CHANGELOG.md</files>
  <action>
**package.json version:** Change version from `1.20.5` to `0.1.0`. This signals:
- 0.x = pre-1.0 fork with its own versioning
- Not confused with upstream 1.20.x releases
- SemVer-compliant

Also update the description from `"GSD command definitions and agent prompts for the pilot autonomous development pipeline. Fork of get-shit-done v1.20.5."` to `"Autonomy-first GSD system for the Pilot development pipeline. Forked from get-shit-done, adapted for zero-interactivity."` — shorter, positions the fork correctly.

Keep the `bin.get-shit-done-cc` entry unchanged for now (compatibility — the installer is shared infrastructure and changing the bin name would break existing users; document this in package.json with a comment-equivalent note... actually JSON has no comments, so leave it and let the README explain this).

**CHANGELOG.md:** 
1. Add a new section at the top of the file, after `## [Unreleased]`, clearly separating fork history from upstream history:

```markdown
## [0.1.0] - 2026-03-07

### pilot-gsd Fork Release

This is the first independent release of pilot-gsd, forked from [get-shit-done](https://github.com/gsd-build/get-shit-done) v1.20.5.

#### Added (fork)
- Zero-interactivity: all AskUserQuestion calls removed or guarded with auto-mode bypasses
- Explicit model assignments in agent frontmatter (opus for creators, sonnet for checkers)
- Autonomous config defaults (yolo mode, all gates off, auto_advance)
- gsd-delegate command for Pilot runner integration
- gsd-judge command for execution verdict
- Upstream GSD sync (v1.20.5 merged)
- Prompt hardening: positive framing, dead file cleanup, @ reference validation

#### Changed (fork)
- Frontmatter format: opencode-native (model:, color: hex, tools: YAML object)
- Path convention: .opencode/ instead of ~/.claude/
- Slash commands: /gsd-* instead of /gsd:*
- All workflows have auto-mode guards for autonomous pipeline operation

#### Attribution
Forked from get-shit-done v1.20.5 by TÂCHES (Lex Christopherson).
See upstream changelog entries below for pre-fork history.

---

*Upstream changelog entries below are preserved for historical reference.*
*Release links point to the original project repository.*

---
```

2. At the bottom of the file, update the link reference definitions:
   - Add: `[0.1.0]: https://github.com/lucafanselau/pilot-gsd/releases/tag/v0.1.0`
   - Change `[Unreleased]` link to: `https://github.com/lucafanselau/pilot-gsd/compare/v0.1.0...HEAD`
   - Keep ALL existing upstream release links unchanged (they correctly point to the upstream repo — that's where those releases live)
  </action>
  <verify>
1. `node -e "console.log(require('./package.json').version)"` outputs `0.1.0`
2. `grep "0.1.0" CHANGELOG.md` shows the new fork release section
3. `grep "TÂCHES" CHANGELOG.md` confirms upstream attribution preserved
4. `grep "lucafanselau/pilot-gsd" CHANGELOG.md` shows fork-specific links
5. `grep "glittercowboy/get-shit-done" CHANGELOG.md` confirms upstream links preserved (for upstream-era entries)
  </verify>
  <done>Version is 0.1.0 (distinct from upstream). CHANGELOG has fork release section with attribution. Upstream history preserved with correct links.</done>
</task>

</tasks>

<verification>
- `grep -rn "glittercowboy" SECURITY.md .github/CODEOWNERS .github/FUNDING.yml` returns zero matches
- `node -e "console.log(require('./package.json').version)"` returns `0.1.0`  
- `grep "Lex Christopherson" LICENSE` confirms license attribution preserved
- CHANGELOG.md has both fork section and preserved upstream entries
</verification>

<success_criteria>
All project metadata files reference fork identity. Version 0.1.0 is distinct from upstream 1.20.x. Attribution to original project is preserved in LICENSE, CHANGELOG, and SECURITY.md. No upstream-owner references remain in files where they would mislead users.
</success_criteria>

<output>
After completion, create `.planning/phases/11-fork-release-cleanup-identity-attribution-security-hygiene-and-readme/11-01-SUMMARY.md`
</output>
