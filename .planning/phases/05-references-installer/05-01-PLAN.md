---
phase: 05-references-installer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/get-shit-done/references/model-profiles.md
  - .opencode/get-shit-done/references/checkpoints.md
  - .opencode/get-shit-done/references/questioning.md
  - bin/install.js
autonomous: true

must_haves:
  truths:
    - "model-profiles.md explains that agent frontmatter model: field takes precedence over profile system"
    - "checkpoints.md documents that all checkpoints are auto-handled in autonomous mode"
    - "questioning.md notes it is only used in interactive mode (discuss-phase)"
    - "bin/install.js has pilot-gsd fork documentation comment after shebang"
  artifacts:
    - path: ".opencode/get-shit-done/references/model-profiles.md"
      provides: "Fork note about frontmatter model precedence"
      contains: "pilot-gsd fork"
    - path: ".opencode/get-shit-done/references/checkpoints.md"
      provides: "Rule #6 about auto-handled checkpoints"
      contains: "pilot-gsd fork"
    - path: ".opencode/get-shit-done/references/questioning.md"
      provides: "Fork note about autonomous mode skip"
      contains: "pilot-gsd fork"
    - path: "bin/install.js"
      provides: "Pilot-gsd fork documentation comment"
      contains: "pilot-gsd fork"
  key_links: []
---

<objective>
Add pilot-gsd fork documentation notes to 3 reference files and 1 installer file.

Purpose: Requirements REF-01, REF-02, REF-03, INST-01 — document fork-specific behavior (autonomous mode, frontmatter model precedence) so users and agents understand how this fork differs from upstream GSD.

Output: 4 modified files with fork-specific notes added at prescribed locations.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fork notes to 3 reference files</name>
  <files>
    .opencode/get-shit-done/references/model-profiles.md
    .opencode/get-shit-done/references/checkpoints.md
    .opencode/get-shit-done/references/questioning.md
  </files>
  <action>
    **model-profiles.md (REF-01):** Add the following blockquote at the very top of the file, before the `# Model Profiles` heading:

    ```
    > **pilot-gsd fork:** Models are set directly in agent frontmatter (`model:` field).
    > The profile system below is preserved for upstream compatibility but is not used
    > when agents have explicit model assignments. The frontmatter `model:` takes precedence.

    ```

    (Include a blank line after the blockquote before the heading.)

    **checkpoints.md (REF-02):** Inside the `<overview>` section, after golden rule #4 (the line starting with `4. **Secrets come from user`), add:

    ```
    5. **pilot-gsd fork:** ALL checkpoints are auto-handled in autonomous mode. human-verify auto-approves, decision auto-selects first option. human-action checkpoints log a warning and skip (the pipeline cannot provide auth credentials or physical access).
    ```

    Note: The current file has rules 1-4. The requirements spec says "after rule #5" but the file only has 4 rules currently. Add as rule 5 (not 6). If there are already 5 rules when you read it, add as rule 6.

    **questioning.md (REF-03):** Add the following blockquote at the very top of the file, before the `<questioning_guide>` tag:

    ```
    > **pilot-gsd fork:** This reference is only used in interactive mode (discuss-phase).
    > Autonomous pipeline commands (new-project --auto, plan-phase --auto) skip all questioning.

    ```

    (Include a blank line after the blockquote before the tag.)
  </action>
  <verify>
    - `grep -c 'pilot-gsd fork' .opencode/get-shit-done/references/model-profiles.md` returns 1
    - `grep -c 'pilot-gsd fork' .opencode/get-shit-done/references/checkpoints.md` returns 1
    - `grep -c 'pilot-gsd fork' .opencode/get-shit-done/references/questioning.md` returns 1
  </verify>
  <done>All 3 reference files contain their fork-specific documentation notes at the correct locations</done>
</task>

<task type="auto">
  <name>Task 2: Add fork documentation comment to bin/install.js</name>
  <files>bin/install.js</files>
  <action>
    Add the following comment block immediately after the shebang line (`#!/usr/bin/env node`), before the existing `const fs = require('fs');` line:

    ```javascript
    // NOTE (pilot-gsd fork): The Pilot CLI manages installation via symlinks.
    // `pilot setup` symlinks from this repo into project .opencode/ directories.
    // This installer is preserved for standalone/upstream-compatible use.
    // For pilot users: run `pilot setup` instead of this installer.
    ```

    Insert a blank line between the comment block and the existing `const fs` line.
  </action>
  <verify>
    - `grep -c 'pilot-gsd fork' bin/install.js` returns 1
    - `node -c bin/install.js` exits 0 (valid JavaScript syntax)
  </verify>
  <done>bin/install.js has the pilot-gsd fork documentation comment after the shebang, and the file still parses as valid JavaScript</done>
</task>

</tasks>

<verification>
All 4 requirements satisfied:
- `grep -rn 'pilot-gsd fork' .opencode/get-shit-done/references/ bin/install.js` shows exactly 4 matches (one per file)
- No other files modified
- No functional changes (all additions are comments/documentation)
</verification>

<success_criteria>
- REF-01: model-profiles.md has blockquote about frontmatter model field precedence
- REF-02: checkpoints.md has rule about auto-handled checkpoints in autonomous mode
- REF-03: questioning.md has blockquote about autonomous mode skip
- INST-01: bin/install.js has pilot-gsd fork comment after shebang
- All files remain syntactically valid
</success_criteria>

<output>
After completion, create `.planning/phases/05-references-installer/05-01-SUMMARY.md`
</output>
