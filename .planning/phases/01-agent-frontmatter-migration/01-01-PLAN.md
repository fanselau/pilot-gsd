---
phase: 01-agent-frontmatter-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-executor.md
  - agents/gsd-planner.md
  - agents/gsd-phase-researcher.md
  - agents/gsd-project-researcher.md
  - agents/gsd-roadmapper.md
  - agents/gsd-debugger.md
  - agents/gsd-plan-checker.md
  - agents/gsd-verifier.md
  - agents/gsd-integration-checker.md
  - agents/gsd-research-synthesizer.md
  - agents/gsd-codebase-mapper.md
autonomous: true

must_haves:
  truths:
    - "All 11 agent files have no name: field in frontmatter"
    - "All 11 agent files have model: field with correct model assignment"
    - "All 11 agent files have tools: as YAML object (not comma-separated string)"
    - "All 11 agent files have color: as hex value (not color name)"
    - "No /gsd: references remain anywhere in agent files (frontmatter or body)"
    - "No ~/.claude/ references remain anywhere in agent files"
  artifacts:
    - path: "agents/gsd-executor.md"
      provides: "Opus agent with opencode-native frontmatter"
      contains: "model: anthropic/claude-opus-4-6"
    - path: "agents/gsd-planner.md"
      provides: "Opus agent with opencode-native frontmatter"
      contains: "model: anthropic/claude-opus-4-6"
    - path: "agents/gsd-phase-researcher.md"
      provides: "Opus agent with opencode-native frontmatter"
      contains: "model: anthropic/claude-opus-4-6"
    - path: "agents/gsd-project-researcher.md"
      provides: "Opus agent with opencode-native frontmatter"
      contains: "model: anthropic/claude-opus-4-6"
    - path: "agents/gsd-roadmapper.md"
      provides: "Opus agent with opencode-native frontmatter"
      contains: "model: anthropic/claude-opus-4-6"
    - path: "agents/gsd-debugger.md"
      provides: "Opus agent with opencode-native frontmatter"
      contains: "model: anthropic/claude-opus-4-6"
    - path: "agents/gsd-plan-checker.md"
      provides: "Sonnet agent with opencode-native frontmatter"
      contains: "model: anthropic/claude-sonnet-4-6"
    - path: "agents/gsd-verifier.md"
      provides: "Sonnet agent with opencode-native frontmatter"
      contains: "model: anthropic/claude-sonnet-4-6"
    - path: "agents/gsd-integration-checker.md"
      provides: "Sonnet agent with opencode-native frontmatter"
      contains: "model: anthropic/claude-sonnet-4-6"
    - path: "agents/gsd-research-synthesizer.md"
      provides: "Sonnet agent with opencode-native frontmatter"
      contains: "model: anthropic/claude-sonnet-4-6"
    - path: "agents/gsd-codebase-mapper.md"
      provides: "Sonnet agent with opencode-native frontmatter"
      contains: "model: anthropic/claude-sonnet-4-6"
  key_links:
    - from: "agents/*.md frontmatter"
      to: "opencode agent loader"
      via: "YAML frontmatter parsing"
      pattern: "^model: anthropic/claude-(opus|sonnet)-4-6$"
    - from: "agents/*.md frontmatter tools"
      to: "opencode tool registry"
      via: "YAML object format"
      pattern: "^  \\w+: true$"
---

<objective>
Migrate all 11 agent files from upstream GSD frontmatter format to opencode-native format, and replace all deprecated path/command references in body text.

Purpose: Agent files must use opencode-native frontmatter (no name:, model: field, tools: as object, color: as hex) so opencode can parse and spawn them correctly. Body text references must use project-relative paths (./.opencode/) and hyphenated slash commands (/gsd-) matching opencode conventions.

Output: 11 updated agent files with valid opencode frontmatter and corrected body text references.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/pilot-gsd-fork.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 6 opus-model agent frontmatters + body text</name>
  <files>
    agents/gsd-executor.md
    agents/gsd-planner.md
    agents/gsd-phase-researcher.md
    agents/gsd-project-researcher.md
    agents/gsd-roadmapper.md
    agents/gsd-debugger.md
  </files>
  <action>
For each of the 6 opus-model agent files, make these changes:

**Frontmatter changes (between `---` delimiters):**

1. **Remove** the `name:` line entirely
2. **Fix** the `description:` field — replace any `/gsd:` with `/gsd-` within it
3. **Add** `model: anthropic/claude-opus-4-6` after the description line
4. **Replace** `color:` value with hex equivalent:
   - `yellow` → `"#FFFF00"` (executor)
   - `green` → `"#00FF00"` (planner)
   - `cyan` → `"#00FFFF"` (phase-researcher, project-researcher)
   - `purple` → `"#800080"` (roadmapper)
   - `orange` → `"#FFA500"` (debugger)
5. **Replace** `tools:` comma-separated string with YAML object. Tool mapping:

   **gsd-executor** — `tools: Read, Write, Edit, Bash, Grep, Glob` becomes:
   ```yaml
   tools:
     read: true
     write: true
     edit: true
     bash: true
     grep: true
     glob: true
   ```

   **gsd-planner** — `tools: Read, Write, Bash, Glob, Grep, WebFetch, mcp__context7__*` becomes:
   ```yaml
   tools:
     read: true
     write: true
     bash: true
     glob: true
     grep: true
     webfetch: true
     mcp__context7__*: true
   ```

   **gsd-phase-researcher** — `tools: Read, Write, Bash, Grep, Glob, WebSearch, WebFetch, mcp__context7__*` becomes:
   ```yaml
   tools:
     read: true
     write: true
     bash: true
     grep: true
     glob: true
     websearch: true
     webfetch: true
     mcp__context7__*: true
   ```

   **gsd-project-researcher** — `tools: Read, Write, Bash, Grep, Glob, WebSearch, WebFetch, mcp__context7__*` becomes:
   ```yaml
   tools:
     read: true
     write: true
     bash: true
     grep: true
     glob: true
     websearch: true
     webfetch: true
     mcp__context7__*: true
   ```

   **gsd-roadmapper** — `tools: Read, Write, Bash, Glob, Grep` becomes:
   ```yaml
   tools:
     read: true
     write: true
     bash: true
     glob: true
     grep: true
   ```

   **gsd-debugger** — `tools: Read, Write, Edit, Bash, Grep, Glob, WebSearch` becomes:
   ```yaml
   tools:
     read: true
     write: true
     edit: true
     bash: true
     grep: true
     glob: true
     websearch: true
   ```

**Body text changes (everything after the closing `---`):**

6. Replace ALL occurrences of `/gsd:` with `/gsd-` throughout the body text (NOT in code block paths like `~/.claude/get-shit-done/bin/gsd-tools.cjs` — those are file paths, not slash commands)
7. Replace ALL occurrences of `~/.claude/` with `./.opencode/` throughout the body text

**IMPORTANT:** The `~/.claude/` → `./.opencode/` replacement applies to ALL occurrences including:
- `@~/.claude/get-shit-done/` → `@./.opencode/get-shit-done/`
- `node ~/.claude/get-shit-done/bin/` → `node ./.opencode/get-shit-done/bin/`
- Template references like `~/.claude/get-shit-done/templates/` → `./.opencode/get-shit-done/templates/`

Do NOT change any other content in the files — preserve all role definitions, instructions, and behavior exactly.
  </action>
  <verify>
Run these verification commands:
```bash
# No name: field in any opus agent
for f in agents/gsd-executor.md agents/gsd-planner.md agents/gsd-phase-researcher.md agents/gsd-project-researcher.md agents/gsd-roadmapper.md agents/gsd-debugger.md; do
  grep -n '^name:' "$f" && echo "FAIL: $f still has name:" || echo "OK: $f"
done

# All have model: opus
for f in agents/gsd-executor.md agents/gsd-planner.md agents/gsd-phase-researcher.md agents/gsd-project-researcher.md agents/gsd-roadmapper.md agents/gsd-debugger.md; do
  grep -q 'model: anthropic/claude-opus-4-6' "$f" && echo "OK: $f has opus model" || echo "FAIL: $f missing opus model"
done

# No /gsd: references (should be /gsd-)
for f in agents/gsd-executor.md agents/gsd-planner.md agents/gsd-phase-researcher.md agents/gsd-project-researcher.md agents/gsd-roadmapper.md agents/gsd-debugger.md; do
  grep -n '/gsd:' "$f" && echo "FAIL: $f still has /gsd:" || echo "OK: $f"
done

# No ~/.claude/ references
for f in agents/gsd-executor.md agents/gsd-planner.md agents/gsd-phase-researcher.md agents/gsd-project-researcher.md agents/gsd-roadmapper.md agents/gsd-debugger.md; do
  grep -n '~/.claude/' "$f" && echo "FAIL: $f still has ~/.claude/" || echo "OK: $f"
done

# Frontmatter parses (check tools: is indented object, not inline string)
for f in agents/gsd-executor.md agents/gsd-planner.md agents/gsd-phase-researcher.md agents/gsd-project-researcher.md agents/gsd-roadmapper.md agents/gsd-debugger.md; do
  grep -q '^tools:$' "$f" && echo "OK: $f has object-style tools:" || echo "FAIL: $f tools: not object format"
done
```
All checks must output OK with no FAIL lines.
  </verify>
  <done>
6 opus-model agents (executor, planner, phase-researcher, project-researcher, roadmapper, debugger) have:
- No name: field
- model: anthropic/claude-opus-4-6
- tools: as YAML object with correct tool set
- color: as hex value
- All /gsd: replaced with /gsd- (frontmatter and body)
- All ~/.claude/ replaced with ./.opencode/ (body)
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate 5 sonnet-model agent frontmatters + body text</name>
  <files>
    agents/gsd-plan-checker.md
    agents/gsd-verifier.md
    agents/gsd-integration-checker.md
    agents/gsd-research-synthesizer.md
    agents/gsd-codebase-mapper.md
  </files>
  <action>
For each of the 5 sonnet-model agent files, make these changes:

**Frontmatter changes (between `---` delimiters):**

1. **Remove** the `name:` line entirely
2. **Fix** the `description:` field — replace any `/gsd:` with `/gsd-` within it
3. **Add** `model: anthropic/claude-sonnet-4-6` after the description line
4. **Replace** `color:` value with hex equivalent:
   - `green` → `"#00FF00"` (plan-checker, verifier)
   - `blue` → `"#0000FF"` (integration-checker)
   - `purple` → `"#800080"` (research-synthesizer)
   - `cyan` → `"#00FFFF"` (codebase-mapper)
5. **Replace** `tools:` comma-separated string with YAML object. Tool mapping:

   **gsd-plan-checker** — `tools: Read, Bash, Glob, Grep` becomes:
   ```yaml
   tools:
     read: true
     bash: true
     glob: true
     grep: true
   ```

   **gsd-verifier** — `tools: Read, Write, Bash, Grep, Glob` becomes:
   ```yaml
   tools:
     read: true
     write: true
     bash: true
     grep: true
     glob: true
   ```

   **gsd-integration-checker** — `tools: Read, Bash, Grep, Glob` becomes:
   ```yaml
   tools:
     read: true
     bash: true
     grep: true
     glob: true
   ```

   **gsd-research-synthesizer** — `tools: Read, Write, Bash` becomes:
   ```yaml
   tools:
     read: true
     write: true
     bash: true
   ```

   **gsd-codebase-mapper** — `tools: Read, Bash, Grep, Glob, Write` becomes:
   ```yaml
   tools:
     read: true
     bash: true
     grep: true
     glob: true
     write: true
   ```

**Body text changes (everything after the closing `---`):**

6. Replace ALL occurrences of `/gsd:` with `/gsd-` throughout the body text (NOT in code block paths like `~/.claude/get-shit-done/bin/gsd-tools.cjs` — those are file paths, not slash commands)
7. Replace ALL occurrences of `~/.claude/` with `./.opencode/` throughout the body text

**IMPORTANT:** The `~/.claude/` → `./.opencode/` replacement applies to ALL occurrences including:
- `node ~/.claude/get-shit-done/bin/` → `node ./.opencode/get-shit-done/bin/`
- Template references like `~/.claude/get-shit-done/templates/` → `./.opencode/get-shit-done/templates/`
- `@~/.claude/get-shit-done/` → `@./.opencode/get-shit-done/`

Do NOT change any other content in the files — preserve all role definitions, instructions, and behavior exactly.
  </action>
  <verify>
Run these verification commands:
```bash
# No name: field in any sonnet agent
for f in agents/gsd-plan-checker.md agents/gsd-verifier.md agents/gsd-integration-checker.md agents/gsd-research-synthesizer.md agents/gsd-codebase-mapper.md; do
  grep -n '^name:' "$f" && echo "FAIL: $f still has name:" || echo "OK: $f"
done

# All have model: sonnet
for f in agents/gsd-plan-checker.md agents/gsd-verifier.md agents/gsd-integration-checker.md agents/gsd-research-synthesizer.md agents/gsd-codebase-mapper.md; do
  grep -q 'model: anthropic/claude-sonnet-4-6' "$f" && echo "OK: $f has sonnet model" || echo "FAIL: $f missing sonnet model"
done

# No /gsd: references (should be /gsd-)
for f in agents/gsd-plan-checker.md agents/gsd-verifier.md agents/gsd-integration-checker.md agents/gsd-research-synthesizer.md agents/gsd-codebase-mapper.md; do
  grep -n '/gsd:' "$f" && echo "FAIL: $f still has /gsd:" || echo "OK: $f"
done

# No ~/.claude/ references
for f in agents/gsd-plan-checker.md agents/gsd-verifier.md agents/gsd-integration-checker.md agents/gsd-research-synthesizer.md agents/gsd-codebase-mapper.md; do
  grep -n '~/.claude/' "$f" && echo "FAIL: $f still has ~/.claude/" || echo "OK: $f"
done

# Frontmatter parses (check tools: is indented object, not inline string)
for f in agents/gsd-plan-checker.md agents/gsd-verifier.md agents/gsd-integration-checker.md agents/gsd-research-synthesizer.md agents/gsd-codebase-mapper.md; do
  grep -q '^tools:$' "$f" && echo "OK: $f has object-style tools:" || echo "FAIL: $f tools: not object format"
done
```
All checks must output OK with no FAIL lines.
  </verify>
  <done>
5 sonnet-model agents (plan-checker, verifier, integration-checker, research-synthesizer, codebase-mapper) have:
- No name: field
- model: anthropic/claude-sonnet-4-6
- tools: as YAML object with correct tool set
- color: as hex value
- All /gsd: replaced with /gsd- (frontmatter and body)
- All ~/.claude/ replaced with ./.opencode/ (body)
  </done>
</task>

</tasks>

<verification>
Run comprehensive verification across ALL 11 agent files:

```bash
# 1. No name: field in any agent
echo "=== Check: No name: field ==="
grep -l '^name:' agents/gsd-*.md && echo "FAIL" || echo "PASS: No name: fields found"

# 2. All agents have model: field
echo "=== Check: model: field present ==="
for f in agents/gsd-*.md; do
  grep -q '^model:' "$f" || echo "FAIL: $f missing model:"
done
echo "PASS (if no FAIL above)"

# 3. Correct model assignment (6 opus, 5 sonnet)
echo "=== Check: Correct model assignments ==="
for f in agents/gsd-executor.md agents/gsd-planner.md agents/gsd-phase-researcher.md agents/gsd-project-researcher.md agents/gsd-roadmapper.md agents/gsd-debugger.md; do
  grep -q 'anthropic/claude-opus-4-6' "$f" || echo "FAIL: $f should be opus"
done
for f in agents/gsd-plan-checker.md agents/gsd-verifier.md agents/gsd-integration-checker.md agents/gsd-research-synthesizer.md agents/gsd-codebase-mapper.md; do
  grep -q 'anthropic/claude-sonnet-4-6' "$f" || echo "FAIL: $f should be sonnet"
done
echo "PASS (if no FAIL above)"

# 4. All tools: are object format (line ends with just "tools:" not "tools: Something")
echo "=== Check: tools: object format ==="
grep '^tools:' agents/gsd-*.md | grep -v '^[^:]*:tools:$' && echo "FAIL: Some tools: not object format" || echo "PASS"

# 5. All colors are hex
echo "=== Check: color: is hex ==="
grep '^color:' agents/gsd-*.md | grep -v '"#' && echo "FAIL: Some colors not hex" || echo "PASS"

# 6. No /gsd: anywhere
echo "=== Check: No /gsd: references ==="
grep -rn '/gsd:' agents/gsd-*.md && echo "FAIL" || echo "PASS: No /gsd: found"

# 7. No ~/.claude/ anywhere
echo "=== Check: No ~/.claude/ references ==="
grep -rn '~/.claude/' agents/gsd-*.md && echo "FAIL" || echo "PASS: No ~/.claude/ found"

# 8. Count agents (should be 11)
echo "=== Check: Agent count ==="
ls agents/gsd-*.md | wc -l
```

All checks must PASS. Agent count must be 11.
</verification>

<success_criteria>
- All 11 agent files have opencode-native frontmatter (no name:, has model:, tools: as object, color: as hex)
- 6 agents assigned anthropic/claude-opus-4-6: executor, planner, phase-researcher, project-researcher, roadmapper, debugger
- 5 agents assigned anthropic/claude-sonnet-4-6: plan-checker, verifier, integration-checker, research-synthesizer, codebase-mapper
- Zero occurrences of /gsd: across all agent files
- Zero occurrences of ~/.claude/ across all agent files
- All YAML frontmatter is valid (no syntax errors)
- Requirements AGENT-01 through AGENT-06 fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-agent-frontmatter-migration/01-01-SUMMARY.md`
</output>
