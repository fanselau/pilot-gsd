---
phase: 10-upstream-sync
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - .opencode/agents/gsd-codebase-mapper.md
  - .opencode/agents/gsd-debugger.md
  - .opencode/agents/gsd-executor.md
  - .opencode/agents/gsd-integration-checker.md
  - .opencode/agents/gsd-phase-researcher.md
  - .opencode/agents/gsd-plan-checker.md
  - .opencode/agents/gsd-planner.md
  - .opencode/agents/gsd-project-researcher.md
  - .opencode/agents/gsd-research-synthesizer.md
  - .opencode/agents/gsd-roadmapper.md
  - .opencode/agents/gsd-verifier.md
  - .opencode/get-shit-done/workflows/plan-phase.md
  - .opencode/get-shit-done/workflows/execute-phase.md
  - .opencode/get-shit-done/workflows/execute-plan.md
autonomous: true

must_haves:
  truths:
    - "Post-merge integrity check passes against SYSTEM-AUDIT.md"
    - "gsd-tools.cjs works: node get-shit-done/bin/gsd-tools.cjs help exits 0"
    - "Our delegation flow preserved: gsd-delegate.md content matches pre-merge"
    - "Our agent prompts preserved: all 11 agents have pilot frontmatter"
    - "Our frontmatter format preserved: opencode-native, not claude-native"
    - "Our autonomy modifications preserved: no unguarded AskUserQuestion in autonomous paths"
    - "No merge conflict markers left in any file"
    - ".opencode/ copies updated to reflect merged state"
  artifacts:
    - path: ".planning/phases/10-upstream-sync/INTEGRITY-CHECK.md"
      provides: "Post-merge verification report"
      contains: "PASS"
  key_links:
    - from: "SYSTEM-AUDIT.md"
      to: "INTEGRITY-CHECK.md"
      via: "Audit classifications verified against merge results"
      pattern: "ours-only|upstream-take|ours-modified"
    - from: ".opencode/agents/*.md"
      to: "agents/*.md"
      via: ".opencode copies must reflect merged root agents"
      pattern: "model:"
---

<objective>
Verify the merge integrity against the SYSTEM-AUDIT.md and sync the .opencode/ directory with the merged state.

Purpose: The merge is the riskiest operation in this phase. This plan validates that nothing was lost or corrupted, that all pilot customizations survived, and that the .opencode/ pilot-specific copies are updated to incorporate any beneficial upstream changes while keeping our modifications. Also produces FORK-STATUS.md (nice-to-have from requirements).

Output: INTEGRITY-CHECK.md proving the system is healthy, updated .opencode/ files, optional FORK-STATUS.md
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@requirements/upstream-sync.md
@.planning/phases/10-upstream-sync/SYSTEM-AUDIT.md
@.planning/phases/10-upstream-sync/10-01-SUMMARY.md
@.planning/phases/10-upstream-sync/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run integrity checks and produce verification report</name>
  <files>.planning/phases/10-upstream-sync/INTEGRITY-CHECK.md</files>
  <action>
  Run every verification check from requirements/upstream-sync.md and produce INTEGRITY-CHECK.md:

  **Check 1: No conflict markers**
  ```
  grep -rn '<<<<<<\|>>>>>>\|=======' --include='*.md' --include='*.js' --include='*.json' --include='*.cjs' . | grep -v .git/ | grep -v node_modules/
  ```
  Expected: 0 results. Record PASS/FAIL.

  **Check 2: gsd-tools.cjs works**
  ```
  node get-shit-done/bin/gsd-tools.cjs help
  ```
  Expected: exits 0, shows help output. Record PASS/FAIL.

  **Check 3: Our delegation flow preserved**
  Compare key sections of `commands/gsd-delegate.md` (or wherever it ended up) to verify it contains our custom delegation logic (pilot-specific delegation prompt, not upstream default). Specifically check for: our delegator agent, our command routing, our autonomous defaults.
  Record PASS/FAIL.

  **Check 4: Agent frontmatter preserved (all 11 files)**
  For each file in `agents/*.md`:
  - Verify `model:` field exists (not `name:`)
  - Verify `tools:` is YAML object format (not comma-separated string)
  - Verify `color:` is hex value
  - Verify no `name:` field in frontmatter
  Record per-file PASS/FAIL.

  **Check 5: Command frontmatter preserved**
  Sample 5 command files and verify:
  - `tools:` is object format (not `allowed-tools:` list)
  - No `AskUserQuestion` in tools
  - No `name:` field
  Record PASS/FAIL.

  **Check 6: Autonomy modifications preserved**
  Grep for unguarded AskUserQuestion in autonomous code paths:
  ```
  grep -rn 'AskUserQuestion' --include='*.md' get-shit-done/workflows/ | grep -v 'auto_advance\|auto.*mode\|yolo\|#\|<!--'
  ```
  Expected: 0 unguarded instances (some guarded instances are OK). Record PASS/FAIL.

  **Check 7: @ references intact**
  ```
  grep -rn '@\./' --include='*.md' .opencode/ get-shit-done/ commands/ agents/ | grep -v node_modules | head -20
  ```
  Verify @ references point to valid files. Spot-check 10 references. Record PASS/FAIL.

  **Check 8: No broken file references**
  For key referenced files, verify they exist:
  - `get-shit-done/bin/gsd-tools.cjs` — exists
  - `get-shit-done/workflows/execute-plan.md` — exists
  - `get-shit-done/workflows/plan-phase.md` — exists
  - `get-shit-done/templates/summary.md` — exists
  Record PASS/FAIL.

  **Check 9: Audit classification verification**
  Cross-reference SYSTEM-AUDIT.md:
  - Files classified `upstream-take` should match upstream version (spot-check 3 files with `git diff HEAD upstream/main -- <file>`)
  - Files classified `ours-only` should be unchanged from pre-merge (spot-check 3 files)
  - Files classified `ours-modified` should have our modifications preserved (spot-check agents + commands)
  Record PASS/FAIL.

  **Output format for INTEGRITY-CHECK.md:**
  ```
  # Post-Merge Integrity Check
  Date: {date}
  Merge commit: {hash}

  ## Results

  | # | Check | Result | Notes |
  |---|-------|--------|-------|
  | 1 | No conflict markers | PASS/FAIL | {details} |
  | 2 | gsd-tools.cjs works | PASS/FAIL | {details} |
  ...

  ## Summary
  {N}/9 checks passed
  Overall: PASS/FAIL

  ## Issues Found (if any)
  - {issue description and remediation}
  ```
  </action>
  <verify>
  - INTEGRITY-CHECK.md exists
  - Contains results table with all 9 checks
  - Contains overall PASS/FAIL summary
  - If any FAIL: issues section has remediation steps
  </verify>
  <done>
  INTEGRITY-CHECK.md exists with 9/9 checks passing (or issues documented with fixes applied).
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync .opencode/ directory with merged state</name>
  <files>
  .opencode/agents/*.md
  .opencode/get-shit-done/bin/gsd-tools.cjs
  .opencode/get-shit-done/workflows/*.md
  .opencode/get-shit-done/references/*.md
  .opencode/get-shit-done/templates/*.md
  .opencode/command/*.md
  </files>
  <action>
  The `.opencode/` directory contains pilot-specific copies of files. After the merge, root files (`agents/`, `get-shit-done/`, `commands/`) have been updated. The `.opencode/` copies need to be synced.

  **Strategy:**

  For `.opencode/agents/` (11 files):
  - These are our pilot-modified agents. The root `agents/` files were kept as ours during merge (pilot frontmatter).
  - Copy root agents to .opencode/agents: `cp agents/*.md .opencode/agents/`
  - This ensures .opencode/ has the latest version matching root.

  For `.opencode/get-shit-done/bin/`:
  - Create the directory if it does not exist: `mkdir -p .opencode/get-shit-done/bin/`
  - Copy gsd-tools.cjs: `cp get-shit-done/bin/gsd-tools.cjs .opencode/get-shit-done/bin/`
  - Copy test file: `cp get-shit-done/bin/gsd-tools.test.cjs .opencode/get-shit-done/bin/`
  - Verify: `node .opencode/get-shit-done/bin/gsd-tools.cjs help` exits 0

  For `.opencode/get-shit-done/workflows/`:
  - These have our auto-mode guards. The root `get-shit-done/workflows/` files were kept as ours where we had patches.
  - Copy all: `cp get-shit-done/workflows/*.md .opencode/get-shit-done/workflows/`
  - For any NEW workflow files upstream added (not in our .opencode/ yet): these get copied too.

  For `.opencode/get-shit-done/references/`:
  - Copy all: `cp get-shit-done/references/*.md .opencode/get-shit-done/references/`
  - This picks up new reference files from upstream and updates existing ones.

  For `.opencode/get-shit-done/templates/`:
  - Copy directory recursively: `cp -r get-shit-done/templates/* .opencode/get-shit-done/templates/`
  - This picks up new template files from upstream.

  For `.opencode/command/`:
  - These are pilot's command files. They correspond to `commands/*.md` (our flat structure).
  - Copy: `for f in commands/gsd-*.md; do cp "$f" ".opencode/command/$(basename $f)"; done`
  - Also copy pilot-judge.md if it exists: `cp commands/pilot-judge.md .opencode/command/ 2>/dev/null`
  - For gsd-delegate.md: `cp commands/gsd-delegate.md .opencode/command/ 2>/dev/null`

  **After all copies, verify key files:**
  - `node .opencode/get-shit-done/bin/gsd-tools.cjs help` exits 0
  - `diff .opencode/agents/gsd-planner.md agents/gsd-planner.md` shows no differences
  - `ls .opencode/get-shit-done/bin/gsd-tools.cjs` exists

  **Commit the .opencode/ sync:**
  ```
  git add .opencode/
  git commit -m "sync: update .opencode/ copies to match merged upstream state

  - Agents: synced 11 files with pilot frontmatter
  - Workflows: synced with auto-mode guards
  - References: synced with fork notes
  - Templates: synced with upstream additions
  - Commands: synced with pilot format
  - bin/gsd-tools.cjs: copied to .opencode/ (was missing)"
  ```
  </action>
  <verify>
  - `node .opencode/get-shit-done/bin/gsd-tools.cjs help` exits 0
  - `diff .opencode/agents/gsd-planner.md agents/gsd-planner.md` returns no differences
  - `diff .opencode/agents/gsd-executor.md agents/gsd-executor.md` returns no differences
  - `ls .opencode/get-shit-done/bin/gsd-tools.cjs` file exists
  - `diff .opencode/get-shit-done/workflows/plan-phase.md get-shit-done/workflows/plan-phase.md` returns no differences
  </verify>
  <done>
  .opencode/ directory is fully synced with the merged root state. gsd-tools.cjs is available at .opencode/get-shit-done/bin/gsd-tools.cjs. All .opencode/ copies match their root counterparts.
  </done>
</task>

</tasks>

<verification>
- [ ] INTEGRITY-CHECK.md exists with 9/9 checks passing
- [ ] `node .opencode/get-shit-done/bin/gsd-tools.cjs help` exits 0
- [ ] `node get-shit-done/bin/gsd-tools.cjs help` exits 0
- [ ] .opencode/agents/ files match root agents/
- [ ] .opencode/get-shit-done/bin/gsd-tools.cjs exists
- [ ] No conflict markers anywhere in repo
- [ ] Safety backup branch `pre-upstream-sync-backup` exists
</verification>

<success_criteria>
Post-merge integrity check passes (9/9). .opencode/ directory fully synced. gsd-tools.cjs works from both paths. System is ready for continued development.
</success_criteria>

<output>
After completion, create `.planning/phases/10-upstream-sync/10-03-SUMMARY.md`
</output>
