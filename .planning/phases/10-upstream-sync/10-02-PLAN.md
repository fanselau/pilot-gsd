---
phase: 10-upstream-sync
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - agents/gsd-codebase-mapper.md
  - agents/gsd-debugger.md
  - agents/gsd-executor.md
  - agents/gsd-integration-checker.md
  - agents/gsd-phase-researcher.md
  - agents/gsd-plan-checker.md
  - agents/gsd-planner.md
  - agents/gsd-project-researcher.md
  - agents/gsd-research-synthesizer.md
  - agents/gsd-roadmapper.md
  - agents/gsd-verifier.md
  - bin/install.js
  - get-shit-done/bin/gsd-tools.cjs
  - get-shit-done/bin/gsd-tools.test.cjs
  - package.json
  - package-lock.json
  - README.md
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "git merge upstream/main completed with all conflicts resolved"
    - "No merge conflict markers remain in any file"
    - "gsd-tools.cjs exists and runs: node get-shit-done/bin/gsd-tools.cjs help exits 0"
    - "Our agent frontmatter preserved: model:, tools: object, hex color:, no name: field"
    - "Our command frontmatter preserved: tools: object, no AskUserQuestion"
    - "Our auto-mode guards in workflows preserved"
    - "Our delegation prompt (gsd-delegate.md) is exactly our version"
  artifacts:
    - path: "get-shit-done/bin/gsd-tools.cjs"
      provides: "GSD tooling script from upstream"
      contains: "gsd-tools"
    - path: "bin/install.js"
      provides: "Upstream installer"
  key_links:
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: ".planning/"
      via: "CLI tool reads planning state"
      pattern: "gsd-tools"
    - from: "agents/*.md"
      to: ".opencode/agents/*.md"
      via: "pilot copies use our frontmatter format"
      pattern: "model:"
---

<objective>
Execute `git merge upstream/main` and resolve all conflicts using the SYSTEM-AUDIT.md classification as a deterministic guide.

Purpose: This is the core merge — 707 upstream commits into our fork. Every conflict must be resolved according to the classification from Plan 01. Infrastructure/tooling comes from upstream; our custom prompting and frontmatter format stays.

Output: Clean merge commit with no conflict markers, all upstream tooling working, all pilot customizations preserved.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@requirements/upstream-sync.md
@.planning/phases/10-upstream-sync/SYSTEM-AUDIT.md
@.planning/phases/10-upstream-sync/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute merge and resolve all conflicts</name>
  <files>All files touched by upstream merge (147 files changed per diff stat)</files>
  <action>
  **Step 1: Create safety branch**
  ```
  git branch pre-upstream-sync-backup
  ```
  This gives us a rollback point.

  **Step 2: Start the merge**
  ```
  git merge upstream/main --no-commit
  ```
  Use `--no-commit` to inspect the merge state before finalizing. This stages all clean merges and leaves conflicts marked.

  **Step 3: Identify all conflicts**
  ```
  git diff --name-only --diff-filter=U
  ```
  List every unresolved conflict file.

  **Step 4: Resolve conflicts by classification**

  Reference SYSTEM-AUDIT.md for each conflicting file. Apply these rules:

  **For `upstream-take` files (accept upstream entirely):**
  ```
  git checkout --theirs <file>
  git add <file>
  ```
  Files: `get-shit-done/bin/gsd-tools.cjs`, `get-shit-done/bin/gsd-tools.test.cjs`, `bin/install.js`, `package.json`, `package-lock.json`, `.gitignore`, `CHANGELOG.md`, `SECURITY.md`, `LICENSE`, `README.md`, all `assets/`, `docs/`, `hooks/`, `scripts/`, `.github/` files.

  For `.gitignore`: After taking upstream version, verify our `.opencode/` and `.planning/` entries are present. If not, append them.

  **For `ours-only` files (keep ours, upstream shouldn't touch these):**
  If any of these are in conflict (shouldn't be), take ours:
  ```
  git checkout --ours <file>
  git add <file>
  ```
  Files: Everything in `.opencode/`, `requirements/`, `opencode.json`.

  **For `ours-modified` files (manual merge — ours wins, incorporate upstream structure):**

  These are the hard ones. For each:

  *Agent files (agents/*.md):*
  - Take OUR version entirely (`git checkout --ours agents/*.md && git add agents/*.md`)
  - Our frontmatter (model:, tools: object, hex color:, no name:) MUST be preserved
  - If upstream added new content sections in agent body text, consider incorporating AFTER merge is complete as a separate step — but frontmatter is non-negotiable

  *Command files (commands/*.md or commands/gsd/*.md):*
  - **Critical: Upstream moved from `commands/gsd-*.md` to `commands/gsd/*.md` subdirectory**
  - Keep our flat `commands/` structure (our frontmatter and format)
  - For commands that exist in both: take ours (`git checkout --ours`)
  - For NEW commands only in upstream (e.g., `commands/gsd/debug.md`, `commands/gsd/execute-phase.md`, `commands/gsd/health.md`, `commands/gsd/quick.md`): these are new — accept upstream version but note they need frontmatter conversion in Plan 03 or a follow-up phase
  - Handle the directory structure mismatch: if upstream added `commands/gsd/` subdirectory, resolve by keeping our flat structure and placing new upstream commands in `commands/` with `gsd-` prefix

  *Workflow files (get-shit-done/workflows/*.md):*
  - Take OURS for files where we have auto-mode guards
  - For files upstream significantly rewrote: take upstream, then re-apply our auto-mode patches. These patches are documented in requirements/pilot-gsd-fork.md and applied during Phases 1-5.
  - For new workflow files only in upstream: accept them as-is (they're additions)
  - Key files to keep ours: `execute-phase.md`, `plan-phase.md`, `new-project.md`, `quick.md`, `execute-plan.md`, `transition.md`, `pause-work.md`

  *Reference files (get-shit-done/references/*.md):*
  - Take upstream version, then re-add our fork notes (REF-01, REF-02, REF-03) if they got removed
  - For new reference files: accept upstream as-is

  *Template files (get-shit-done/templates/*):*
  - Take upstream for most templates
  - For `config.json`: take upstream structure but ensure our yolo/gates-off settings are present
  - For new templates: accept upstream as-is

  **For `shared` files:**
  Read both versions and manually merge, preferring upstream for infrastructure and ours for prompting content.

  **Step 5: Handle directory structure changes**
  Upstream may have reorganized `commands/gsd/*.md` (subdirectory) vs our `commands/gsd-*.md` (flat). Ensure our flat structure is preserved. Any new upstream commands in `commands/gsd/` should be copied to `commands/` with appropriate naming.

  **Step 6: Clean up conflict markers**
  After all resolutions:
  ```
  grep -rn '<<<<<<' --include='*.md' --include='*.js' --include='*.json' --include='*.cjs' .
  ```
  Must return 0 results. If any found, resolve them.

  **Step 7: Stage and verify**
  ```
  git add -A
  git status
  ```
  Verify all conflicts resolved, no unmerged paths remain.

  **Step 8: Finalize merge commit**
  ```
  git commit -m "merge: upstream/main into dev (707 commits)

  Conflict resolution per SYSTEM-AUDIT.md:
  - Infrastructure/tooling: upstream version (gsd-tools.cjs, install.js, package.json)
  - Agent prompts: our version (pilot frontmatter format preserved)
  - Command files: our version (pilot frontmatter, no AskUserQuestion)
  - Workflows: our version where auto-mode guards exist
  - References/templates: upstream with fork notes re-applied"
  ```
  </action>
  <verify>
  - `grep -rn '<<<<<<' --include='*.md' --include='*.js' --include='*.json' --include='*.cjs' . | grep -v .git/` returns 0 results
  - `node get-shit-done/bin/gsd-tools.cjs help` exits 0
  - `git log -1 --oneline` shows merge commit
  - `git diff --name-only --diff-filter=U` returns empty (no unresolved conflicts)
  - Agent file spot check: `head -10 agents/gsd-planner.md` shows `model:` field, no `name:` field
  - Command file spot check: `head -10 commands/gsd-plan-phase.md` shows our frontmatter format
  </verify>
  <done>
  Merge commit exists. Zero conflict markers in any file. gsd-tools.cjs runs successfully. Agent and command frontmatter is pilot format. No unresolved merge paths.
  </done>
</task>

</tasks>

<verification>
- [ ] `git merge upstream/main` completed (merge commit exists)
- [ ] `grep -rn '<<<<<<' . --include='*.md' --include='*.js' --include='*.json' --include='*.cjs' | grep -v .git` returns 0 results
- [ ] `node get-shit-done/bin/gsd-tools.cjs help` exits 0
- [ ] `head -10 agents/gsd-planner.md` shows model: field, no name: field
- [ ] `head -10 agents/gsd-executor.md` shows model: field, tools: object format
- [ ] All 11 agent files have pilot frontmatter format
- [ ] `git branch --contains pre-upstream-sync-backup` exists as rollback point
</verification>

<success_criteria>
Clean merge commit with 707 upstream commits integrated. All infrastructure/tooling matches upstream. All pilot customizations (frontmatter format, auto-mode guards, delegation prompt) preserved exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/10-upstream-sync/10-02-SUMMARY.md`
</output>
