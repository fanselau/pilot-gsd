---
phase: 12-agents-md-management-setup-lessons-and-health
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - commands/gsd-delegate.md
autonomous: true
requirements:
  - AGENTS-DELEGATE-01

must_haves:
  truths:
    - "The delegate AI knows about gsd-setup-agents and gsd-lessons commands for routing"
    - "Delegation routing correctly identifies when to use setup-agents vs lessons vs other commands"
  artifacts:
    - path: "commands/gsd-delegate.md"
      provides: "Updated delegation routing with new commands"
      contains: "gsd-setup-agents"
  key_links:
    - from: "commands/gsd-delegate.md"
      to: "commands/gsd-setup-agents.md"
      via: "command routing table"
      pattern: "setup-agents"
    - from: "commands/gsd-delegate.md"
      to: "commands/gsd-lessons.md"
      via: "command routing table"
      pattern: "lessons"
---

<objective>
Update the delegate AI to know about the two new AGENTS.md management commands so that job routing works correctly.

Purpose: The `gsd-delegate.md` is the routing brain that decides which commands to run for a given job. It needs to know about `gsd-setup-agents` and `gsd-lessons` to route appropriately.

Output: Updated `commands/gsd-delegate.md` with new command entries.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/agents-md-management.md
@commands/gsd-delegate.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update gsd-delegate.md with new commands</name>
  <files>commands/gsd-delegate.md</files>
  <action>
Update the Available Commands table in `commands/gsd-delegate.md` to include the two new commands:

Add these rows to the command table:

| `setup-agents` | `[project-path]` | Analyze codebase and generate minimal AGENTS.md. Run when no AGENTS.md exists or user wants to regenerate. |
| `lessons` | `[project-path]` | Extract actionable lessons from build history into candidate buffer. Run after phases complete, especially after failures. |

These are standalone commands (like `quick`) — they don't participate in the plan/execute/verify cycle. The delegate should route to them when:
- `setup-agents`: Job description mentions "AGENTS.md", "agent instructions", "setup agents", or project has no AGENTS.md and user wants one
- `lessons`: Job description mentions "lessons", "extract lessons", "what went wrong", or after a phase with failures

The delegate should output these as single-step plans:
```json
{
  "reasoning": "Project needs AGENTS.md generated from codebase analysis.",
  "steps": [
    { "command": "setup-agents", "args": "" }
  ]
}
```

Make sure the command names in the delegate table match the actual command file names minus the `gsd-` prefix (consistent with existing entries like `quick`, `health`).

Also add a brief note in the "Scope: quick" section or equivalent that `setup-agents` and `lessons` are standalone commands like `quick` — they produce artifacts directly without plan/execute ceremony.
  </action>
  <verify>
- `grep "setup-agents" commands/gsd-delegate.md` returns matches in the command table
- `grep "lessons" commands/gsd-delegate.md` returns matches in the command table
- Both entries have correct arg format and description
- No syntax errors in the markdown table
  </verify>
  <done>
`commands/gsd-delegate.md` includes `setup-agents` and `lessons` in its Available Commands table with correct descriptions, and routing guidance for when to use each command.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final validation of all Phase 12 deliverables</name>
  <files>commands/gsd-setup-agents.md, commands/gsd-lessons.md, commands/gsd-delegate.md</files>
  <action>
Validate all three files created/modified in Phase 12:

1. **Frontmatter validation** for both new commands:
   - Parse YAML frontmatter of `commands/gsd-setup-agents.md` and `commands/gsd-lessons.md`
   - Confirm `description` field exists and is descriptive
   - Confirm `tools` includes `read: true`, `glob: true`, `grep: true`, `write: true`
   - Confirm `bash` is NOT in tools (per requirements)

2. **Content validation** for both new commands:
   - `gsd-setup-agents.md` mentions all 4 AGENTS.md sections: Commands, Stack, Structure, Boundaries
   - `gsd-setup-agents.md` mentions 20-40 line target and max 60 constraint
   - `gsd-setup-agents.md` mentions not auto-committing
   - `gsd-lessons.md` mentions `.planning/LESSONS-CANDIDATES.md` output target
   - `gsd-lessons.md` mentions never auto-committing to AGENTS.md
   - `gsd-lessons.md` has good/bad lesson examples

3. **Delegate validation:**
   - `commands/gsd-delegate.md` contains `setup-agents` and `lessons` entries
   - Entries are in the correct table format

4. **Cross-reference check:**
   - Verify new command files are discoverable: `ls commands/gsd-setup-agents.md commands/gsd-lessons.md`

If any validation fails, fix the issue in the relevant file.
  </action>
  <verify>
- All three files exist and have valid structure
- `grep -c "bash" commands/gsd-setup-agents.md` returns 0 (no bash tool)
- `grep -c "bash" commands/gsd-lessons.md` returns 0 (no bash tool)
- `grep -c "setup-agents" commands/gsd-delegate.md` returns at least 1
- `grep -c "LESSONS-CANDIDATES" commands/gsd-lessons.md` returns at least 1
  </verify>
  <done>
All Phase 12 deliverables validated: two new command files with correct frontmatter and content, delegate updated with routing for both commands.
  </done>
</task>

</tasks>

<verification>
- `commands/gsd-delegate.md` references both new commands
- All three files parse correctly (valid YAML frontmatter, valid markdown)
- No broken references between delegate and command files
- Command names are consistent (gsd-setup-agents, gsd-lessons)
</verification>

<success_criteria>
- Delegate routes correctly to new commands
- All files validated end-to-end
- Phase 12 deliverables are complete and ready for use
</success_criteria>

<output>
After completion, create `.planning/phases/12-agents-md-management-setup-lessons-and-health/12-02-SUMMARY.md`
</output>
