---
phase: 04-workflow-interactivity-stripping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/new-project.md
autonomous: true
requirements:
  - WFLOW-01
  - WFLOW-07
  - WFLOW-08

must_haves:
  truths:
    - "new-project.md Step 2a uses hardcoded autonomous defaults instead of AskUserQuestion rounds"
    - "new-project.md Step 5 is skipped entirely in auto mode"
    - "All remaining AskUserQuestion calls in new-project.md Steps 6-9 have auto-mode guards"
  artifacts:
    - path: "get-shit-done/workflows/new-project.md"
      provides: "Autonomous-safe new-project workflow"
      contains: "Use optimal defaults without asking"
  key_links:
    - from: "get-shit-done/workflows/new-project.md"
      to: ".planning/config.json"
      via: "hardcoded defaults in Step 2a"
      pattern: "Use optimal defaults without asking"
---

<objective>
Strip all interactive AskUserQuestion calls from new-project.md workflow or guard them with auto-mode bypasses.

Purpose: new-project.md is the most interactive workflow (7+ AskUserQuestion calls across multiple steps). In autonomous mode, every one of these blocks on stdin and hangs the pipeline. This plan eliminates all blocking paths.
Output: new-project.md with zero unguarded AskUserQuestion calls in autonomous code paths
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/pilot-gsd-fork.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Step 2a AskUserQuestion rounds with hardcoded defaults (WFLOW-01)</name>
  <files>get-shit-done/workflows/new-project.md</files>
  <action>
Replace lines 82-200 (Step 2a: Auto Mode Config) in new-project.md.

Current Step 2a (lines 82-200) has two AskUserQuestion rounds collecting 7 config settings (Depth, Execution, Git Tracking in Round 1; Research, Plan Check, Verifier, AI Models in Round 2), then creates config.json from responses.

Replace the ENTIRE Step 2a content (from "## 2a. Auto Mode Config" through the "Proceed to Step 4" line at ~200) with:

```markdown
## 2a. Auto Mode Config (auto mode only)

**If auto mode:** Use optimal defaults without asking. No questions — fully autonomous.

YOLO mode is implicit (auto = YOLO).

Create `.planning/config.json` with autonomous defaults:

```json
{
  "mode": "yolo",
  "depth": "quick",
  "parallelization": true,
  "commit_docs": true,
  "model_profile": "balanced",
  "workflow": {
    "research": true,
    "plan_check": true,
    "verifier": true,
    "auto_advance": true
  }
}
```

**Commit config.json:**

```bash
mkdir -p .planning
node ./.opencode/get-shit-done/bin/gsd-tools.cjs commit "chore: add project config" --files .planning/config.json
```

**Persist auto-advance to config (survives context compaction):**

```bash
node ./.opencode/get-shit-done/bin/gsd-tools.cjs config-set workflow.auto_advance true
```

Proceed to Step 4 (skip brownfield offer in auto mode — already handled in Step 2).
```

Important:
- Remove ALL AskUserQuestion calls from Step 2a (both Round 1 and Round 2)
- The hardcoded config uses sensible defaults: yolo mode, quick depth, parallel on, commit docs on, balanced models, all workflow agents on, auto_advance on
- Keep the config.json commit and auto-advance persist commands
- Ensure the closing instruction sends to Step 4 (skipping Step 3 interactive questioning)
  </action>
  <verify>
Read the modified new-project.md and confirm:
1. Step 2a has NO AskUserQuestion calls
2. Step 2a has a hardcoded config.json with yolo/quick/autonomous defaults
3. Step 2a still commits config.json and persists auto_advance
4. The section transitions to Step 4
  </verify>
  <done>Step 2a uses hardcoded defaults with zero interactive prompts</done>
</task>

<task type="auto">
  <name>Task 2: Add auto-mode guards to remaining AskUserQuestion calls (WFLOW-07, WFLOW-08)</name>
  <files>get-shit-done/workflows/new-project.md</files>
  <action>
Add auto-mode bypass guards before every remaining AskUserQuestion call in new-project.md that isn't already protected. Scan the file for ALL remaining `AskUserQuestion` references and add guards.

**Step 5 (Workflow Preferences, ~line 344):**
Already has "**If auto mode:** Skip — config was collected in Step 2a." at line 346. Verify this guard exists. If it does, no change needed for WFLOW-07.

**Step 6 (Research Decision, ~line 506):**
Has `AskUserQuestion` for research decision. Already has "**If auto mode:** Default to 'Research first' without asking." at line 504. Verify this guard exists. No change needed if present.

**Step 7 (Define Requirements, ~lines 770-835):**
Multiple AskUserQuestion calls for:
- Feature scoping per category (~line 810-820)
- Gap identification (~line 829)
- Requirements confirmation (~line 882-885)

Before each AskUserQuestion block in Step 7, add:
```markdown
**If auto mode:** [specific auto behavior] and continue.
```

Specific behaviors:
- Feature scoping: "**If auto mode:** Auto-include all table stakes features and features explicitly mentioned in provided document. Auto-defer differentiators not mentioned. Skip per-category questioning."
- Gap identification: "**If auto mode:** Skip — auto mode assumes research covered all requirements."
- Requirements confirmation: "**If auto mode:** Auto-approve and commit directly."

Note: Some of these guards may already exist (lines 766-772 have auto-mode handling). Check first, only add what's missing.

**Step 8 (Create Roadmap, ~lines 982-1014):**
Has AskUserQuestion for roadmap approval. Already has "**If auto mode:** Skip approval gate — auto-approve and commit directly." at line 980. Verify this guard exists.

**Step 9 (Done, ~line 1053):**
Has `SlashCommand("/gsd:discuss-phase 1 --auto")` invocation. This is fine — it's auto-advancing, not asking.

**General pattern for adding guards:**
Before any unguarded AskUserQuestion block, add a line:
```markdown
**If auto mode:** Use default (first option) and continue.
```
  </action>
  <verify>
Search the entire new-project.md for `AskUserQuestion` and verify each occurrence either:
1. Is inside a step that's entirely skipped in auto mode (Steps 3, 5), OR
2. Has an explicit "If auto mode:" guard BEFORE it, OR
3. Is in the interactive-only code path

Run: `grep -n 'AskUserQuestion' get-shit-done/workflows/new-project.md` and verify every hit is guarded.
  </verify>
  <done>Every AskUserQuestion in new-project.md has an auto-mode bypass or is in an interactive-only section</done>
</task>

</tasks>

<verification>
1. `grep -n 'AskUserQuestion' get-shit-done/workflows/new-project.md` — every occurrence must be inside auto-mode guarded section or interactive-only step
2. Step 2a contains hardcoded config.json with yolo defaults (no questions)
3. No code path in auto mode can reach an unguarded AskUserQuestion
</verification>

<success_criteria>
- Step 2a has zero AskUserQuestion calls and uses hardcoded autonomous defaults
- All AskUserQuestion calls in Steps 5-9 have auto-mode guards
- Auto-mode config.json matches requirements spec (yolo, quick, auto_advance true, all gates off)
- File still works correctly in interactive mode (guards don't break non-auto paths)
</success_criteria>

<output>
After completion, create `.planning/phases/04-workflow-interactivity-stripping/04-01-SUMMARY.md`
</output>
